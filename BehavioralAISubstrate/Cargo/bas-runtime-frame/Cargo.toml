[package]
name = "bas-runtime-frame"
description = "chapter 七百三 第四刀 — Rust port of the L14 sovereign frame aggregator + per-turn observation bundle + thought-fold typed surface"
license.workspace = true
edition.workspace = true
version.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

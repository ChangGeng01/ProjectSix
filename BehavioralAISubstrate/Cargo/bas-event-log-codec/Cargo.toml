[package]
name = "bas-event-log-codec"
description = "chapter 七百三 第四刀 — Rust port of BASEventLogEntry encode/decode + replay-determinism byte-stability test harness"
license.workspace = true
edition.workspace = true
version.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

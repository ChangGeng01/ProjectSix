{
 "12": {
  "id": 12,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.88,
  "file": "Tests/BehavioralAISubstrateTests/BASTraceExitDeviceTests.swift:100",
  "summary": "The B3 trace-exit device A/B genuinely needs hardware only for the raw Row data (real quality parity, token cuts, tok/s from running Qwen3.5-4B + MTP weights). But the PROMOTION VERDICT — the arithmetic that decides \"quality held AND think-cut positive AND budget-arm answered\" — is pure code (summarize() at :109-120) that is print-only, returns Void, and is asserted nowhere. That verdict logic is fully Mac-verifiable with synthetic rows and is currently untested; extracting it and asserting on it also converts the device run itself from plumbing-only to teeth.",
  "mac_layer_verified": "Two layers are already solidly Mac-verified. (1) The pure stop-rule state machine BASTraceExitPolicy.observe — entropy convergence, budget guard, marker-gating, self-close disarm, one-shot latch, nil-entropy transparency, primed-in-think — is covered by BASTraceExitPolicyTests.swift with adversarial reversals (high-entropy holds fire, no-markers-never-fires, exact reserve-line landing). (2) The production arming/config resolution MLXOrganAdapter._traceExitConfig (capped arms, uncapped off, kill-switch, reserve scales with cap, budget-only) is covered by BASTraceExitProductionConfigTests.swift.",
  "device_only_layer": "The raw Row values themselves — real answer-correctness parity, real think/total-token cuts, real decode seconds/tok-s, and whether the entropy rule actually fires on live trunk-entropy signals — are irreducibly device-only: they require loading Qwen3.5-4B-4bit + qwen35_mtp_folded.safetensors from Documents and running the fused MTP spec decoder against the real distribution. No Mac substitute produces those numbers (the harness is gated on BAS_TRACE_AB=1 + staged device weights).",
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "description": "The A/B promotion-verdict computation is pure Swift over the Row struct but exists only as side-effecting print() inside summarize(); no function returns a verdict and nothing asserts it. A bug in the verdict math (e.g. quality-parity comparison inverted, think-cut sign flipped, budget-arm 'answered' check dropped) would ship silently — the human reading the printed table is the only gate, on device AND (hypothetically) on Mac.",
   "fix_teeth_sketch": "Extract a pure `enum/struct BASTraceExitABVerdict { static func evaluate(entropy: [Row], budget: [Row]) -> (qualityHeld: Bool, thinkCut: Double, budgetAnswered: Bool, promote: Bool) }` encoding the documented promotion criteria: (a) entropy regime — exit.correct >= ctrl.correct (quality parity is THE gate, comment lines 18-19) and mean(exit.think) < mean(ctrl.think) (positive think-cut); (b) budget regime — count(exit.answerLen>0) >= count(ctrl.answerLen>0) (the 96-tok co-gate fix). Then two teeth: (1) Mac unit test feeding SYNTHETIC Row arrays — a 'good' A/B promotes; an adversarially-reversed regression (exit quality 6/6→2/6, or exit budget-answered→0, or exit thinks MORE than ctrl) must flip promote=false and red the test (survives reversal, not a fixed-default green). (2) Change the device test's terminal `XCTAssertFalse(rows.isEmpty)` at :100 to call the same evaluate() on the real rows and XCTAssert(verdict.qualityHeld) / XCTAssert(verdict.thinkCut > 0) — converting the device run from plumbing-only to a real quality/perf gate that reds on regression instead of being human-read."
  },
  "false_green_risk": {
   "exists": false,
   "description": "The existing policy and config tests are real teeth (they include adversarial reversals — high-entropy-holds-fire, kill-switch-nil, exact-reserve-line). No test here passes by asserting a macOS/Swift default value. The device test is not a false-green in the 'wrong default' sense; it is a genuine plumbing-only test (see below)."
  },
  "plumbing_only_test": {
   "exists": true,
   "description": "BASTraceExitDeviceTests.testTraceExitDeviceAB asserts only `XCTAssertFalse(rows.isEmpty)` (line 100) — always true once the harness runs, since rows are unconditionally appended. summarize() (:109-120) and the exit-fires count (:101-102) are print()-only. A real regression — exit arm loses answer correctness (quality gate, the whole point per comment lines 18-19), think-cut goes to zero/negative, or the budget arm stops emitting an answer — passes green. The promotion verdict lives entirely in printed output, so even a full device run does not fail on a quality/perf regression."
  },
  "recommended_action": "Mac-side work IS available now (do not close as device-only): (1) extract the print-only summarize() math into a pure BASTraceExitABVerdict.evaluate() and add a Mac unit test with synthetic Rows that reds under an adversarially-reversed regression (quality drop / negative think-cut / budget-arm-unanswered). (2) Replace the device test's `XCTAssertFalse(rows.isEmpty)` at line 100 with assertions on that same evaluate() result so the device run becomes a real gate. Only the raw Row numbers remain device-only; the verdict LOGIC that decides promote/regress is Mac-testable and currently untested."
 },
 "5": {
  "id": 5,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.85,
  "file": "DeviceTestApp/Sources/App/BASProbeCommon.swift:526",
  "summary": "idleGuardedSleep (BASProbeCommon.swift:526) is the shared cooldown helper called by runSamplingVerify's two 30s inter-arm cooldowns (BASQwen35MTPProbe.swift:264,275) plus ~6 other probes. Only the iOS-27 compile is claimed done; the true lock-screen-suspension behavior is device-only. BUT there is a real UNCLOSED Mac-side defect: the shared helper's spin loop uses wall-clock Date() (lines 529/531), while the endurance runner's identical spin was deliberately hardened to the MONOTONIC clock monoNowNs()/DispatchTime.uptimeNanoseconds (BASEnduranceAppRunner.swift:2581, per audit LOW-6) precisely because an NTP/DST wall-clock step mid-cooldown can cut the cooldown short or stall the loop. The commit's own docstring claims it uses \"the SAME knob the endurance runner uses,\" but it regressed the clock-source hardening — a forward wall-clock jump during the inter-arm cooldown shortens thermal recovery and biases the very tok/s ratio runSamplingVerify measures, which is exactly the \"instrument self-corruption\" class this commit set out to kill. No existing test references idleGuardedSleep (0 hits in Tests/), so there is no false-green to reverse; DeviceTestApp is an xcodegen app with NO unit-test target, which is the honest friction for adding teeth.",
  "mac_layer_verified": "iOS-27 SDK compile of the env-gated branch (spin vs Task.sleep) and the seconds>0 guard — pure control-flow, compiler-checked. The choice of cooldown mechanism (spin under BAS_COOLDOWN_SPIN=1 vs Task.sleep otherwise) and the clock-source correctness of the spin's termination bound are pure logic, fully Mac-verifiable — no hardware needed to reason about them.",
  "device_only_layer": "The irreducible device behavior: that a bare Task.sleep in a devicectl-launched app actually SUSPENDS indefinitely once the phone locks (the freeze pit) while the sync spin keeps the process schedulable, AND that the GPU still thermally rests during a busy e-core spin. Both the suspension trip and the thermal-recovery-under-spin claim need a real locked iPhone under devicectl — not observable on macOS.",
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "fix_teeth_sketch": "FIX (trivial): replace the two Date() calls at BASProbeCommon.swift:529/531 with the monotonic source the endurance runner already uses — endNs = DispatchTime.now().uptimeNanoseconds &+ UInt64(seconds*1e9); while DispatchTime.now().uptimeNanoseconds < endNs { ... } — so the shared helper matches the LOW-6 hardening its own docstring claims. TEETH: extract the termination decision into a pure, injectable helper, e.g. `func spinShouldContinue(nowNs: UInt64, endNs: UInt64) -> Bool` (or make idleGuardedSleep take `nowNs: () -> UInt64` defaulting to DispatchTime). Since DeviceTestApp has no test target, either add a minimal test target to it or hoist this pure helper into a Mac-testable module. Test asserts: (a) with a monotonic clock the loop runs ~seconds and terminates; (b) a simulated BACKWARD clock step mid-window does NOT stall it and a FORWARD jump does NOT cut it short. ADVERSARIAL REVERSAL: revert the helper to Date() → the backward/forward-step tests must red. That guarantees the test has teeth rather than passing on the happy path."
  },
  "false_green_risk": {
   "exists": false
  },
  "plumbing_only_test": {
   "exists": false,
   "description": "There is no device A/B test for this cooldown item at all — no plumbing-only assertion to flag. Separately, runSamplingVerify's own promotion verdict (mS/mP >= 1.15) is a genuine tok/s-ratio verdict, not plumbing, but it measures the MTP sampling lane, not the MED-2 cooldown mechanism."
  },
  "recommended_action": "Mac-side fix available NOW: change idleGuardedSleep's spin bound from wall-clock Date() to the monotonic DispatchTime.uptimeNanoseconds, matching BASEnduranceAppRunner's audit-LOW-6 hardening its docstring already claims — closing an instrument-self-corruption regression (a wall-clock jump during the inter-arm cooldown biases the very tok/s measurement). Give it teeth by hoisting the termination check into a pure, clock-injectable helper and testing backward/forward-step immunity with an adversarial revert-to-Date() red. Only the actual locked-screen suspension trip and thermal-rest-under-spin remain genuinely device-only."
 },
 "8": {
  "id": 8,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.82,
  "file": "Sources/BASAppleAdapters/BASSingleDriverTripwire.swift:45",
  "mac_layer_verified": "Utility BASSingleDriverTripwire (NSLock + inFlight counter + swappable @Sendable onViolation) is Foundation-only and fully Mac-unit-tested (BASSingleDriverTripwireTests): serialized enter/exit never fires, reentrant enter fires once with correct label+count, stray exits never underflow. Install census verified honest on Mac: all 6 stateful decode sessions (Decode/LayerSplit/Mamba3Dual/HybridDecode/Mamba3/Mamba) + BASSaguaroSpeculator carry a driverTripwire, each install correctly #if DEBUG-guarded and bracketing its in-place mutator with enter()/defer exit() spanning the await suspension. Saguaro brackets all 4 state-machine mutators (reset/prefill/propose/commit over pending/draftPos). The 'NOT applicable' exclusion is verifiable and TRUE: BASCoreAIPrefillSession holds only `let model`/`let function`, binds EMPTY MutableViews, keeps no cross-call mutable state — genuinely stateless, correctly uninstrumented.",
  "device_only_layer": "The irreducible hardware behavior: a real concurrent .aimodel `step` on device actually tripping the assertionFailure, and the underlying premise that a second concurrent driver would corrupt the in-place fused KV/SSM state. Loading a CoreAI .aimodel and driving two real forwards needs Xcode-27 CoreAI + Apple silicon; not reproducible on the Mac SwiftPM host (sessions are #if canImport(CoreAI)-gated).",
  "false_green_risk": {
   "exists": false,
   "description": "testConcurrentEntryFiresViolation is a genuine teeth, not a false-green: the documented adversarial reversal (drop the `n > 1` check in enter()) makes the handler never fire, reddening the assertion. It asserts a POSITIVE fired event (count==1, label match, n==2) rather than a passive default value, so disabling the fix reverses it."
  },
  "plumbing_only_test": {
   "exists": false,
   "description": "No device A/B or DeviceTestApp test exists for this item at all (the DeviceTestApp Tripwire hits are unrelated features: RoutedVectorIndexStorage, ModuleLayering). So there is no plumbing-only 'it ran' device test masking a regression here."
  },
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "description": "Every existing tripwire test drives enter()/exit() SYNCHRONOUSLY on one thread. The real contract the sessions rely on is async: enter() -> await run() suspension -> defer exit(), with a SECOND concurrent Task entering during the suspension window. Two Mac-verifiable sub-layers are uncovered by the current synchronous tests: (a) that the wire actually fires when two concurrently-scheduled Tasks race through the enter/await/exit bracket (the true usage pattern), and (b) that the NSLock-guarded inFlight counter is data-race-free under real parallel access (TSAN). Neither needs CoreAI — only the Foundation-only utility plus a trivial fake async driver that mimics the bracket.",
   "fix_teeth_sketch": "Add an async XCTest in BASSingleDriverTripwireTests using a CoreAI-free FakeDriver actor-free class: `func drive() async { tw.enter(); defer { tw.exit() }; await Task.yield()/tiny sleep }`. Launch N=2..k concurrent Tasks via withTaskGroup all calling drive() on the SAME tripwire; await all; assert rec.count >= 1 (a concurrent overlap was caught) and tw.inFlightCount == 0 afterward. Adversarial reversal (drop `n>1`) must drive rec.count to 0 -> test reds. Run the suite under -sanitize=thread to prove the lock is race-free. This closes the gap between 'counter arithmetic is correct' (current) and 'the async-bracket-over-suspension pattern the sessions actually use catches a real concurrent second driver, race-free' — entirely on Mac."
  },
  "recommended_action": "Mac-side work remains: add an async/concurrent-Task teeth to BASSingleDriverTripwireTests (fix+teeth sketch above) exercising the real enter->await->defer-exit bracket under a TaskGroup, with the same adversarial reversal, ideally under ThreadSanitizer. This is the highest-value output and does NOT need a device. Only the actual on-hardware .aimodel concurrent-step trip and the KV-corruption premise are genuinely device-only.",
  "summary": "Item located and well-built: the tripwire UTILITY has genuine adversarial-reversal teeth (no false-green), all 6 decode sessions + Saguaro (4 mutators) are #if DEBUG-installed and bracket their mutators correctly, and the 'PrefillSession not applicable' exclusion is verifiably true (it is genuinely stateless). NOT genuinely device-only: the existing tests are synchronous-only, leaving an unclosed Mac-verifiable sub-layer — the actual async-bracket-over-suspension pattern under real concurrent Tasks (and lock race-freedom under TSAN), which a CoreAI-free fake async driver + TaskGroup test can close on Mac now. Only the real on-device concurrent .aimodel step trip is irreducibly device-only. No plumbing-only device test exists."
 },
 "11": {
  "id": 11,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.82,
  "file": "Sources/BASMetalSubstrate/BASMetalSyncBridge.swift:36",
  "mac_layer_verified": "Single-caller bridge decision logic is real-teeth verified in BASMetalSyncBridgeTests.swift: fast->op result, nil->fallback, timeout->fallback returns before the 600ms slow op (asserts elapsed<0.5), one-shot result box. These would red on an adversarial reversal (not false-green).",
  "device_only_layer": "Real thermal throttling of the Metal path and the uncancellable Metal wedge/task-leak under sustained thermal pressure genuinely need on-device hardware.",
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "fix_teeth_sketch": "The ledger note conflates thermal (device-only) with load (Mac-reproducible). The DispatchSemaphore.wait-on-cooperative-thread priority-inversion / pool-saturation is exercisable on any multicore Mac with no thermal. Add two XCTest teeth: (1) LIVENESS (non-flaky, deterministic because the timeout forces return): launch N = ProcessInfo.processInfo.activeProcessorCount*2 concurrent runWithTimeout calls from inside cooperative Tasks, each op awaiting Task.sleep, and assert every call returns within timeoutMs + epsilon and none hangs unbounded -> proves no unbounded priority inversion. (2) DEGRADATION-WITNESS: under that saturation with a FAST op, assert whether usedFallback stays false or collapses to all-true; documents the starvation pathology and becomes the pass-gate for a fix. A candidate Mac-verifiable fix is decoupling op execution from the blocked caller pool (dedicated DispatchQueue/Thread for the async op, or a precondition that the caller is not on the cooperative executor) so a blocked cooperative thread cannot starve op scheduling -> the degradation test flips to usedFallback==false under saturation."
  },
  "false_green_risk": {
   "exists": false,
   "description": "The 4 existing single-caller tests are genuine teeth: the timeout test asserts the call returns before the 600ms slow op completes, and the nil test asserts fallback selection; both would red if the bridge logic were reversed. No reliance on a macOS default value."
  },
  "plumbing_only_test": {
   "exists": false,
   "description": "No device A/B test exists for this item at all (disposition was left as documented LOW, no post-audit commit), so there is no plumbing-only-passing device test to flag. The gap is the absence of a concurrency test, not a hollow one."
  },
  "recommended_action": "Mac-side fix available now: add a concurrent-saturation liveness + degradation-witness XCTest to BASMetalSyncBridgeTests.swift (N = activeProcessorCount*2 cooperative callers). The liveness half is deterministic (timeout guarantees return) and converts the LOW item from 'documented/unverified' to 'bounded + reproduced on Mac'; the degradation half becomes the pass-gate for any future off-cooperative-pool fix. Only real thermal throttling of the Metal path remains truly device-only.",
  "summary": "id=11 (ledger x-concurrency MED-LOW-10) is BASMetalSyncBridge.runWithTimeout: DispatchSemaphore.wait parks the calling cooperative thread, risking priority inversion / pool starvation. Single-caller decision logic is already real-teeth verified (not false-green). The ledger note wrongly folds 'load' into 'thermal': the pool-saturation property is Mac-reproducible with no device. Unclosed Mac sub-layer = a concurrent-saturation liveness (deterministic) + degradation-witness test; only real thermal throttling is irreducibly device-only. Not genuinely device-only."
 },
 "7": {
  "id": 7,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.82,
  "file": "DeviceTestApp/Sources/App/BASQwen35RdarProbe.swift:393 (fputs to stderr) + :77 (setvbuf(stderr,_IONBF)); helper idleGuardedSleep in DeviceTestApp/Sources/App/BASProbeCommon.swift:518 (sibling MED-2)",
  "mac_layer_verified": "Compile-only; buffering-survival behavior is untested on any platform.",
  "device_only_layer": "Whether the unbuffered \"STEP n\" stderr line actually reaches the devicectl console BEFORE an rdar 177354777 hard crash (a CoreAI/ANE kernel crash), under devicectl's real non-TTY stdio redirection. This needs the physical iPhone Air, the actual crashing asset, and devicectl's block-buffering environment — irreducibly device+crash-survival.",
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "description": "The PREMISE of the fix — that a line written to fully-buffered stdout is swallowed by a hard crash while an unbuffered-stderr line survives — is a pure POSIX/libc property that reproduces identically on macOS and is NOT re-tested by the compile. It can be closed with a host XCTest, adversarially reversible.",
   "fix_teeth_sketch": "Add a macOS-hosted XCTest (in Tests/, #if os(macOS)) that fork/execs a tiny child (or uses a helper main gated by an env var) which: (a) reopens fd1 to a temp file (non-TTY => C stdlib default = fully buffered) and fd2 unbuffered via setvbuf(stderr,nil,_IONBF,0); (b) writes one marker line to stdout and one to stderr; (c) hard-crashes WITHOUT flushing — raise(SIGKILL) or _exit(1) (NOT abort, which may flush). Parent asserts: the stderr marker IS present in the captured fd2 file and the stdout marker is ABSENT. Adversarial reversal = route the marker to buffered stdout (mimicking the reverted print()) => the assertion reds, proving it is not a false-green. This locks the doctrine the fix depends on. Optionally also extract the STEP emit into a 1-line injectable closure so the actual RdarProbe emit-path (not just libc) is exercised, but that is secondary."
  },
  "false_green_risk": {
   "exists": false,
   "description": "No existing test covers this item — verification to date is compile + code-review reasoning only, so there is no green test that could be passing for the wrong reason. NOTE for the proposed teeth: the trap to avoid is using abort() (which flushes stdio on many libcs) or a TTY-backed fd (line-buffered) as the crash primitive — either would make the buffered-stdout line survive and turn the test into a false-green. Must use SIGKILL/_exit + a redirected (pipe/file) fd."
  },
  "plumbing_only_test": {
   "exists": false,
   "description": "No device A/B test exists for this item. The probe emits the STEP line as a raw forensic anchor consumed by a human reading the devicectl console after a crash — there is no rows/verdict assertion to be hollow. The one adjacent risk is a purely cosmetic source-guard test (grep that fputs/stderr is used, not print) which would be a weak plumbing-only guard; the buffering-survival teeth above is strictly stronger and preferred."
  },
  "recommended_action": "Mac-side work IS still available: add the macOS-hosted buffering-survival teeth described above (small effort) to lock the fix's premise with an adversarially-reversible assertion — this is the highest-value close. Caveats to state honestly in the commit: (1) it verifies the libc/POSIX buffering principle the fix relies on, not the probe running end-to-end (the probe needs CoreAI + an on-device .aimodel and cannot run on Mac; the App/probe sources are in NO test target — only substrate package Tests are wired into BASDeviceTests). (2) The remaining irreducible layer — the STEP line actually surviving to the devicectl console ahead of a real rdar 177354777 crash — stays genuinely device-only and should be closed by one real devicectl launch that induces the crash and confirms the last-STEP line appears.",
  "summary": "M-i MED-6 = commit cebea725a: the per-STEP crash-localizer line in BASQwen35RdarProbe was moved from block-buffered print()/stdout to setvbuf(stderr,_IONBF)+fputs so it survives a hard crash under devicectl. LOCATED at BASQwen35RdarProbe.swift:77 (setvbuf) and :393 (fputs). Only the COMPILE layer is Mac-verified; there is NO behavioral test anywhere (probe App sources aren't in any test target). NOT genuinely device-only: the fix's core premise — buffered-stdout line lost on hard crash, unbuffered-stderr line survives — is a POSIX/libc property reproducible on macOS and closable now with a small adversarially-reversible host XCTest (fork a child, redirect fds to non-TTY, write to buffered stdout + unbuffered stderr, SIGKILL/_exit without flush, assert stderr line survived + stdout line lost; reversal to buffered stdout must red — avoid abort()/TTY which flush and would create a false-green). Only the last mile (line reaching the real devicectl console before an actual rdar 177354777 crash) stays device-only. No existing false-green or plumbing-only test to flag."
 },
 "6": {
  "id": 6,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.88,
  "file": "Sources/BASRuntimeCore/BASBurnInDeadline.swift:26 (extracted+tested); DeviceTestApp/Sources/App/BASQwen35RdarProbe.swift:148-181,216-222,256-258 (inline hand-copy, iOS-only)",
  "mac_layer_verified": "The burn-in deadline state machine is extracted to a pure value type BASBurnInDeadline (BASRuntimeCore) and Mac-unit-tested by BASBurnInDeadlineTests (5 tests, unconditional — no XCTSkip / no #if os gating, so it compiles+runs in the macOS test bundle). The tests carry real adversarial teeth against the model: atUnplug at plateau arms the 20-min burn cap NOT the 12-min window (asserts both == burnCap and != window); a plateau outlasting the window is NOT expired at 13min (the exact MED-4 clobber bug); dip→fresh counted window; plugged samples never end burn-in. Disabling the fix inside the model reds these. The inline device copy faithfully mirrors this spec (verified by reading: atUnplug plateau→burnCap, post-unplug `if !burning` re-arm, sampleIfDue dip→zero+restart).",
  "device_only_layer": "Only the irreducible hardware behavior needs a real iPhone: the actual UIDevice.batteryLevel reading, the real duration iOS pins the 100% top-buffer plateau (~10-20 min), the live energy-per-token (%/1k-tok) draw, whether the all-resident 4-asset ANE chain jetsams, and whether the live decode loop consuming battery()/plugged() actually transitions as the model predicts. None of that is reproducible on Mac.",
  "false_green_risk": {
   "exists": true,
   "description": "SYSTEM-LEVEL false-green (not model-level). The teeth tests target the EXTRACTED value type BASBurnInDeadline, which grep confirms has ZERO callers outside the test — runM4 keeps a SEPARATE inline hand-copy of the same state machine (the source comment openly admits 'the inline vars below MIRROR that tested spec ... a full call-through rewire is a device-verified follow-up'). Nothing enforces the two copies stay equal, so reverting the actual inline device fix (dropping the `if !burning` guards at lines 222/258, restoring the unconditional clobber) would NOT red any test. The fix's presence in the real code path is unverified by the green suite. Model-level teeth are genuine (adversarial reversal reds them); the gap is that they verify a decoupled copy."
  },
  "plumbing_only_test": {
   "exists": false,
   "description": "The device runM4 is a logging instrument (writes qwen35-m4-<arm>-*.log), not an A/B test with a rows-non-empty assertion, so the plumbing-only trap does not apply. The Mac tests assert real verdicts (deadline values, expiry, burn state), not merely that logic ran."
  },
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "description": "Two Mac-closable sub-layers remain. (1) The divergence itself: rewire runM4 to CALL BASBurnInDeadline.atUnplug/onSample/isExpired (feeding battery()/plugged() as inputs) instead of the inline hand-copy — this is pure-logic and compile-checkable on Mac and kills the system-level false-green; only the runtime re-verify of the rewired loop is device-gated. (2) Two untested branches of the extracted model: the burn-cap-as-SAFETY-BOUND (isExpired must become TRUE once nowEpoch>=burnCap while a plateau never dips — otherwise a stuck-at-100% battery could hang M4 forever), and onSample while NOT burning (a dip in the counted window must NOT restart it — guard `burning` path).",
   "fix_teeth_sketch": "Add to BASBurnInDeadlineTests: `testBurnCapBoundsAStuckPlateau` — d=atUnplug(now:0,atPlateau:true,window:720,burnCap:1200); after onSample(now:21*60,plugged:false,battery:99.8) (never dips) assert still.burning && still.isExpired(now:21*60) (safety cap ends the stuck run). And `testDipWhileNotBurningDoesNotRestartWindow` — d=atUnplug(atPlateau:false); assert d.onSample(now:5*60,plugged:false,battery:80) == d (unchanged; guard `burning` false). Both red if the respective branch regresses. Larger follow-up: the call-through rewire (device-runtime re-verify required)."
  },
  "recommended_action": "Mac-side work is still available and worth doing: (a) add the two teeth tests above (trivial, closes the burn-cap safety-bound and not-burning-dip branches of the extracted model); (b) rewire runM4 to consume BASBurnInDeadline rather than the inline hand-copy, eliminating the system-level false-green (compile-verifiable on Mac; the rewired loop's live behavior is the only device-gated remainder). Do NOT mark this item genuinely device-only — the transition logic is fully Mac-verifiable and the current green suite does not actually cover the shipped device code path.",
  "summary": "Item is real and partially closed: the burn-in deadline guard (MED-4) is extracted to BASBurnInDeadline and Mac-tested with genuine adversarial teeth (no skip-gating, runs in the macOS bundle). But the teeth verify a DECOUPLED hand-copy — runM4 still uses its own inline copy with zero call-through, so reverting the actual device fix reds nothing (system-level false-green flagged by the doctrine). Unclosed Mac work: (1) add tests for the burn-cap safety-bound and not-burning-dip branches; (2) rewire runM4 to call BASBurnInDeadline to kill the divergence. Only the real battery reading, plateau-hold duration, and live energy-per-token draw are irreducibly device-only. genuinely_device_only = false."
 },
 "2": {
  "id": 2,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.9,
  "file": "Sources/BASMLXAdapter/MLXOrganAdapter+SessionPersist.swift:59",
  "mac_layer_verified": "The shared helper BASSQLiteFileProtection.apply is well-covered on Mac by BASSQLiteFileProtectionTests: it flips a pre-set .complete -> .completeUntilFirstUserAuthentication on the DB + -wal sidecar (adversarial-reversal design, reds if setAttributes is skipped), skips a missing sidecar cleanly, and no-ops under the BAS_FILE_PROTECTION=0 kill-switch. The MEMORY-ATOM call-site is separately closed by testAtomStoreInvokesProtectionHelperAtOpen, which asserts _applyInvocationCount advances (defeating the fileProtectionError==nil false-green). So the helper mechanism + the atom-store wiring are Mac-verified.",
  "device_only_layer": "Only the irreducible iOS behavior: that the persisted KV snapshot file is actually unreadable at rest in the pre-first-unlock window (kernel per-file Data Protection enforcement). On macOS the .protectionKey attribute is stored but inert (FileVault, not per-file), so real at-rest sealing genuinely needs an iOS device.",
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "fix_teeth_sketch": "The SessionPersist call-site itself — the item's OWN named surface — is untested on Mac. _persist (lines 59-61) both (a) invokes the helper on the KV snapshot url and (b) surfaces the setAttributes error via _sessionProtectionFailureHook instead of the old try?-swallow. That hook is referenced by ZERO tests; the atom path got invocation-counter teeth but the session path did not (asymmetric coverage). _persist is buried behind #if canImport(MLXLLM) and needs a real ChatSessionBox+model, so extract the 3-line protect+surface branch into a tiny MLX-free static seam, e.g. static func _protectSnapshot(at url: URL) { if let e = BASSQLiteFileProtection.apply(toDatabaseAt: url.path) { _sessionProtectionFailureHook?(e) } }, and call it from _persist. Then two Mac teeth: (1) invocation+flip — pre-set a temp file to .complete, call _protectSnapshot, assert _applyInvocationCount advanced AND protection flipped to completeUntilFirstUserAuthentication (reds if the call is dropped). (2) THE x-sov #6 HEART — force apply to RETURN an error by making the temp file user-immutable (FileManager.setAttributes .immutableKey=true / chflags UF_IMMUTABLE, owner-settable, no root) so setAttributes throws; install a hook that captures the error, call _protectSnapshot, assert the captured error is non-nil. Reversal: revert to try?-swallow (drop the hook call) -> captured error stays nil -> reds. This finally proves 'the KV-protection failure is surfaced, not swallowed' on Mac, which is exactly x-sov #6's claim and is currently verified nowhere."
  },
  "false_green_risk": {
   "exists": false,
   "description": "The existing protection tests are honestly designed, not false-green: the flip test pre-sets .complete and proves the FLIP (the original bare-readback false-green that asserted the macOS default was already caught and fixed), and the atom-store test uses _applyInvocationCount to defeat the nil-error false-green. The gap here is absence of coverage, not a green-for-wrong-reason test."
  },
  "plumbing_only_test": {
   "exists": true,
   "description": "BASSessionPersistTests (gated behind heavy BAS_SESSION_PERSIST_TEST=1, loads Qwen3.5-4B) DOES exercise _persist -> apply on the real KV snapshot, but asserts only bytes>1M and that the restored context still contains 'Zebulon'. It makes ZERO assertion about the protection helper being invoked on the snapshot or the _sessionProtectionFailureHook firing — so it passes untouched even if the session-KV protection/surfacing were fully reverted. It verifies persist/restore plumbing, not x-sov #6."
  },
  "recommended_action": "Mac-side fix available now (do NOT close as device-only): extract _persist's protect+surface branch into an MLX-free static seam and add two XCTest teeth as sketched — (1) invocation+flip via pre-set .complete, (2) hook-fires-on-error via a user-immutable temp file that forces setAttributes to throw. This closes the SessionPersist half of x-sov #6 on Mac, matching the coverage the memory-atom half already has. Only the at-rest pre-first-unlock unreadability then remains genuinely iOS-device-gated.",
  "summary": "Item LOCATED at MLXOrganAdapter+SessionPersist.swift:59-61 (+ hook decl line 22). The shared helper's flip/kill-switch/sidecar behavior and the memory-atom call-site are Mac-verified, but the SessionPersist call-site — the item's own _sessionProtectionFailureHook surface — has ZERO test coverage: the hook is referenced by no test, and the one heavy session test is plumbing-only (asserts bytes+context, never protection). NOT genuinely device-only: a small extract-to-seam refactor plus two Mac teeth (invocation+flip, and hook-fires-on-error via a user-immutable file that forces setAttributes to throw) would prove x-sov #6's 'error surfaced not swallowed' claim on macOS. Only the iOS at-rest pre-first-unlock unreadability is irreducibly device-only. No false-green in existing tests; the gap is missing coverage."
 },
 "10": {
  "id": 10,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.8,
  "file": "Sources/BASMLXAdapter/MLXOrganAdapter.swift:416",
  "mac_layer_verified": "LOW-14's pure LRU victim selection is fully Mac-tested: BASSessionLaneTests.testFusedTranscriptEvictionPicksLRU (Tests/BehavioralAISubstrateTests/BASSessionLaneTests.swift:75) reaches the internal static _fusedTranscriptEvictionVictim via @testable and pins 4 cases (under-cap no-op / over-cap evict-front / current-key-skipped / no-victim=nil). Confirmed real teeth, not false-green: flipping order.first->order.last(where:) makes case-2 return c!=a and case-3 return d!=b, so the adversarial reversal genuinely reds two assertions.",
  "unclosed_mac_gap": {
   "exists": true,
   "description": "The single-sourcing INVARIANT that the fix is actually about - fusedTranscriptOrder stays in sync with fusedTranscripts.keys across every removal site (_evictBeyondCap:1685, clearSession:1974, clearAllSessions:1995, transition:1438/1479) - is untested on Mac. Only the pure hand-fed victim picker is covered; it can never catch a call-site that bypasses the _dropFusedTranscript funnel, which is the exact latent-growth bug (stale order entry -> eviction drops an already-gone key -> no-op -> transcripts grow unbounded). clearSession/clearAllSessions are non-async pure state ops and the adapter constructs cheaply on Mac (no model load until loadModel), so this is Mac-closable; only blocker is the members are private.",
   "effort": "small",
   "fix_teeth_sketch": "Add an internal seed helper + internal read (fusedTranscriptOrderForTest) on MLXOrganAdapter; new BASSessionLaneTests case: construct adapter, seed K>cap seats, then drive clearSession / clearAllSessions and apply the eviction victim, asserting Set(fusedTranscriptOrder)==Set(fusedTranscripts.keys) and the count invariant after each op. Adversarial reversal: reintroduce a raw fusedTranscripts.removeValue at one site bypassing the funnel -> order diverges -> invariant assertion reds."
  },
  "false_green_risk": {
   "exists": false,
   "description": "The existing LOW-14 pure-function test survives adversarial reversal (order.first->order.last reds cases 2 and 3), so it is not asserting a default/always-true value. No macOS-default false-green here."
  },
  "plumbing_only_test": {
   "exists": false,
   "description": "No device A/B test exists for either item - both shipped as fix-with-note. So there is no rows-non-empty/it-ran plumbing assertion to game; conversely LOW-12's runtime behavior has no device coverage at all yet."
  },
  "device_only_layer": "LOW-12 in full: the experience warm-start/persist/pressure funnel on _streamDraft (Streaming.swift:66-70, inside #if canImport(MLXLLM)) requires a loaded MLX model + a real streaming turn to observe - no Mac teeth possible, mirrors the eager path exactly. Plus LOW-14's over-cap eviction actually firing during real session/streaming decode and real transcript-memory reclaim needs a device.",
  "recommended_action": "Mac-side work still available: close LOW-14's sync-invariant gap by adding an internal test seam (seed helper + internal order read) and a BASSessionLaneTests case that seeds >cap seats, drives clearSession/clearAllSessions and the eviction victim, and asserts Set(order)==Set(keys) after each (reversal: a raw removeValue bypass reds it). LOW-12 has no Mac layer - correctly device/MLX-gated, leave as fix-with-note.",
  "summary": "LOW-14's pure LRU victim picker is genuinely Mac-closed with real (reversal-tested, non-false-green) teeth. The unclosed Mac-verifiable sub-layer is the funnel single-sourcing invariant (order in sync with the dict across all removal sites) - the actual latent-growth bug the fix targets, catchable on Mac via a small internal test seam since clearSession/clearAllSessions are pure ops and the adapter builds without a model. LOW-12 is honestly device/MLX-only. No plumbing-only or false-green device test exists."
 },
 "3": {
  "id": 3,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.85,
  "file": "Sources/BASCSystemBridge/bas_monotonic_nanos.c:394 (bas_wallclock_nanos, fix at :414 mach_continuous_time); test Tests/BehavioralAISubstrateTests/BASWallclockSleepInclusiveTests.swift:20",
  "summary": "The fix (mach_absolute_time -> mach_continuous_time at bas_monotonic_nanos.c:414) is correct and its irreducible sleep-ADVANCE behavior is genuinely device/sleep-gated (on this Mac continuous-absolute gap = 0-1 units, i.e. no accumulated sleep since boot, so no value-level reversal exists). HOWEVER the two existing Mac tests are BOTH FALSE-GREEN: they pass byte-identically under the OLD buggy mach_absolute_time, so neither discriminates the fix. Real Mac-side work remains: replace the false-green with a sleep-conditional discriminating teeth that CANNOT pass for the wrong reason.",
  "mac_layer_verified": "Mac-verified today: (1) C bridge links + returns rc=0 (Package C target, swift build recompiles, no XCFramework); (2) returns a plausible non-zero since-boot value; (3) monotonic non-decreasing across two reads; (4) the non-trivial timebase SCALING branch (numer!=denom) is actually exercised on this Apple-Silicon Mac (mach_timebase = 125/3, ~41.67ns/tick), though only value-plausibility not scaling PRECISION is asserted. All four hold identically under buggy AND fixed code.",
  "false_green_risk": {
   "exists": true,
   "description": "BOTH tests are false-green. testWallclockNeverReadsBehindTheSleepExcludedClock (line 20) asserts wall>=mono with wall read AFTER mono; but CLOCK_UPTIME_RAW and mach_absolute_time share the same sleep-excluded timescale, so under the OLD buggy code wall(absolute, later) >= mono(uptime, earlier) STILL holds -> passes under the reversal. testWallclockBridgeSucceedsAndIsMonotonic likewise passes under both clocks. Measured on this Mac: mach_continuous_time - mach_absolute_time = 0-1 timebase units (pure read-order skew), confirming no runtime value distinguishes the two implementations here. The commit prose is honest ('weak cross-check','device-verified'), but the test file presents these as the fix's coverage and a later reader sees '2 tests exist' = covered, which is untrue."
  },
  "plumbing_only_test": {
   "exists": false,
   "description": "No device A/B/promotion test is involved; the traps here are false-green, not plumbing-only. There is no on-device test asserting merely 'it ran' for this item."
  },
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "trivial",
   "description": "A sleep-CONDITIONAL discriminating teeth can be added with zero new C (Darwin's mach_continuous_time()/mach_absolute_time() are already imported and used in sibling test files e.g. BASCacheLimitABDeviceTests.swift, BASMonotonicNanosTests.swift). It removes the false-green: instead of passing vacuously it either PROVES the fix or honestly abstains.",
   "fix_teeth_sketch": "import Darwin. Compute rawGap = mach_continuous_time() - mach_absolute_time() (accumulated sleep since boot). Read basWall = BASWallclockNanos.rawCNanos() and basMono = BASMonotonicNanos.rawCNanos() interleaved with two raw reads to bound skew. try XCTSkipUnless(rawGap > SLEEP_THRESHOLD, \"machine has not slept since boot; sleep-advance is device-gated\"). When it does NOT skip, assert (basWall - basMono) is within epsilon of rawGap. ADVERSARIAL REVERSAL: under the buggy mach_absolute_time, basWall tracks the sleep-EXCLUDED clock so (basWall - basMono) ~= 0 while rawGap = sleep > 0 -> assertion FAILS. Under the fix they match. Because it XCTSkips (not passes) when rawGap~=0, it can NEVER false-green: it proves the fix on any machine that has slept, and abstains otherwise. Optionally also add a deterministic invariant check that mach_continuous_time() >= mach_absolute_time() always (documents the timescale ordering) but note that alone does not discriminate the fix."
  },
  "device_only_layer": "The irreducible sleep-ADVANCE property: that a bas_wallclock_nanos timestamp straddling a real system sleep advances by the sleep interval (used for attestation-freshness / thermal-resume staleness detection). The ONLY behavioral divergence between mach_continuous_time and mach_absolute_time is actual system sleep, which a unit test process cannot self-induce (pmset sleep would suspend the test host). On a machine with zero accumulated sleep since boot (this Mac: gap = 0-1 units) the two clocks are byte-identical, so a strict deterministic value reversal requires a device/host that has genuinely slept. The scaling-math and bridge layers are NOT part of this device-only residual.",
  "recommended_action": "Mac-side NOW (trivial): add the sleep-conditional discriminating teeth sketched above to BASWallclockSleepInclusiveTests.swift and downgrade/relabel testWallclockNeverReadsBehindTheSleepExcludedClock as a non-discriminating timescale sanity check so it is not mistaken for fix coverage. This kills the false-green and gives opportunistic deterministic coverage on any slept host, with an honest XCTSkip otherwise. The residual true sleep-advance verification remains device-gated (run on an iPhone that has been backgrounded/asleep between two stamps, asserting basWall-basMono ~= observed sleep duration)."
 },
 "4": {
  "id": 4,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.86,
  "file": "Sources/BASRuntimeCore/BASEffortAllocator.swift:69",
  "mac_layer_verified": "The guarded-floor combiner is pure and fully Mac-unit-tested. BASEffortAllocator.resolve(requested:surprise:stakes:headroom:) takes headroom as an injected scalar (no ProcessInfo, no real thermal). Commit 6e89a56ac added the floor at line 69: cap = (base == .guarded && rank(rawCap) < rank(.guarded)) ? .guarded : rawCap — an explicit .guarded is never dropped below .guarded (rank 1); the floor branch only fires when rawCap==.fast (rank 0), i.e. headroom < 0.15 == critical. Teeth present and honest: BASEffortAllocatorTests.testGuardedSurvivesCriticalThermal (headroom=0.0 → applied==.guarded, budget.criticStrength/toolVerificationStrength > .fast's 0); reverting the floor turns applied back to .fast and reds it (survives adversarial reversal, NOT a false-green). testNonGuardedStillYieldsToCriticalThermal pins .deep→.fast. The enum→scalar bridge (ProcessInfo.ThermalState.critical→0.0 and BASThermalLevel.critical→0.0) is separately Mac-tested (testHeadroomForThermalState / testHeadroomForBASThermalLevel).",
  "device_only_layer": "The only irreducibly device-only behavior is a REAL sustained-load thermal throttle actually driving ProcessInfo.thermalState (or BASDeviceState.thermalLevel) to .critical on hardware. That OS-level transition cannot be produced in a Mac unit test — but nothing in the cap/floor/mapping logic depends on it; every layer below the raw OS reading is a pure enum/scalar transform.",
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "trivial",
   "fix_teeth_sketch": "The pure allocator proves the combiner, but the LIVE/composed seam decision 1B protects is UNTESTED for the guarded case. Two live entry points compose enum->BASEffortSignals.headroom(for:)->resolve, both Mac-testable via injection, neither covers requested=.guarded + critical: (1) BASEffortGovernor.plan(for:requested:thermalState:) - its test testThermalCriticalCapsRegardlessOfDemand uses default requested=.auto and asserts .fast; (2) BASBrainChat facade - testThermalCriticalCapsThreadedEffort (BASBrainChatEffortTests.swift:65) uses effort:.auto and asserts the executor is threaded .fast. So no test proves a .guarded request on a .critical device is threaded to the executor as .guarded (dials intact) rather than re-floored/dropped. Close on Mac with a 1-liner at each seam: (a) BASEffortGovernorTests: let p = BASEffortGovernor.plan(for: req(\"x\"), requested: .guarded, runningMSE: 10.0, thermalState: { .critical }, estimateStakes: { _,_ in 1.0 }); XCTAssertEqual(p.applied, .guarded); XCTAssertNil(p.overrideReason) - exercises the REAL .critical->0.0 mapper chained into the floor (allocator teeth only uses a 0.0 literal). (b) BASBrainChatEffortTests: chat(request(health, effort: .guarded, thermal: .critical)) then XCTAssertEqual(cap.effort, .guarded) - proves request.withEffort threads guarded through unchanged on the hottest device. Both red if the floor is reverted or the facade re-floors."
  },
  "false_green_risk": {
   "exists": false,
   "description": "Existing allocator teeth are genuine: testGuardedSurvivesCriticalThermal asserts applied==.guarded AND that budget dials exceed .fast's zeros; reverting the line-69 floor flips applied to .fast and reds it (real adversarial reversal). The floor branch fires only when rawCap==.fast, so headroom=0.0 targets exactly the branch the fix changed. Guarded already survives SERIOUS thermal without the fix (rank(.guarded)==rank(.balanced)==1), so the fix and its test are correctly scoped to the critical branch only - not a default-value green."
  },
  "plumbing_only_test": {
   "exists": false,
   "description": "No device A/B test claims to cover this item; the guarded-floor behavior is asserted at the value level (applied level + concrete criticStrength/toolVerificationStrength deltas), not a rows-non-empty/it-ran assertion. The gap is missing LIVE-seam coverage, not a plumbing-only pass."
  },
  "recommended_action": "Mac-side work IS still available (do not close as device-only). Add two trivial Mac unit tests exercising the composed live path with requested=.guarded under injected .critical thermal: (1) BASEffortGovernorTests asserting BASEffortGovernor.plan(..., requested: .guarded, thermalState: { .critical }).applied == .guarded (chains the real .critical->0.0 mapper into the floor, which the headroom=0.0-literal allocator test skips); (2) BASBrainChatEffortTests asserting a request(effort: .guarded, thermal: .critical) is threaded to the executor as .guarded (capture.effort == .guarded). Only after these does the residue reduce to the genuinely device-only piece: confirming real hardware thermal throttle drives thermalState->.critical under sustained load.",
  "summary": "runtimecore-a #7 guarded-thermal-floor cap logic is pure and Mac-unit-testable by injecting a headroom scalar, and the core combiner IS tested with honest, reversal-surviving teeth (testGuardedSurvivesCriticalThermal). The hint's implicit question (inject snapshot vs real throttle) resolves toward Mac-testable: only the real OS thermal transition to .critical is device-only. UNCLOSED Mac sub-layer (trivial): the LIVE seam decision 1B actually protects — BASEffortGovernor.plan and the BASBrainChat facade composing headroom(for: .critical) -> resolve(requested: .guarded) — is only tested for .auto (which caps to .fast), never for .guarded, so nothing at the live seam guards against a re-floor/threading regression that would strip safety dials on the hottest device. Not genuinely device-only."
 },
 "1": {
  "id": 1,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.85,
  "file": "Sources/BASRuntimeCore/BASSQLiteFileProtection.swift:50",
  "summary": "x-sov #5 (memory-atom DB file Data Protection) is well-covered on the Mac side — the flip teeth correctly defeat the macOS-default readback false-green and an invocation-count test defeats the never-called false-green. BUT the sibling x-sov #6 claim (\"the error is RETURNED / surfaced, not try?-swallowed\"), which is the literal headline of commit 6c600c0c1, has ZERO teeth: every apply() assertion is XCTAssertNil (happy path), so the helper's `catch { return error }` path and both consumers (store.fileProtectionError, SessionPersist._sessionProtectionFailureHook) never see a non-nil error. Reverting the surfacing fix to a swallow keeps the whole suite green. This surfacing arm is Mac-verifiable (force setAttributes to fail via chflags UF_IMMUTABLE) — so this is NOT device-only. The only genuinely device-only remainder is pre-first-unlock at-rest kernel unreadability on iOS (on macOS the .protectionKey attribute is inert/FileVault).",
  "mac_layer_verified": "Verified on macOS by BASSQLiteFileProtectionTests: (1) helper FLIPS .complete → .completeUntilFirstUserAuthentication on the DB file + -wal sidecar (pre-set .complete makes it adversarial — reds if setAttributes is skipped; correctly avoids the macOS-default readback false-green); (2) missing -shm sidecar skipped without error; (3) kill-switch BAS_FILE_PROTECTION=0 = clean no-op leaving .complete untouched; (4) atom store call-site actually INVOKES the helper at open, asserted via _applyInvocationCount (defeats the nil-error false-green, since fileProtectionError is nil both on success AND if the call were absent). Compile + code path on macOS is exercised (the #if os(iOS) guard was dropped precisely to make it unit-testable).",
  "device_only_layer": "Pre-first-unlock at-rest unreadability of the memory-atom SQLite DB + -wal/-shm sidecars: only an iOS device's kernel actually encrypts and seals the file in the pre-first-unlock window under completeUntilFirstUserAuthentication. On macOS the .protectionKey attribute is faithfully STORED but inert (macOS relies on FileVault, not per-file protection), so whether the OS truly makes the payload_json unreadable while the screen is locked can only be proven on hardware.",
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "description": "The x-sov #6 error-SURFACING arm is completely untested. All apply() assertions are XCTAssertNil (happy path); the helper's `catch { return error }` (line 50-51), the store's `self.fileProtectionError = apply(...)` (BASSQLiteMemoryAtomStore.swift:239-240), and SessionPersist's `_sessionProtectionFailureHook` (MLXOrganAdapter+SessionPersist.swift:59-61) never observe a non-nil error in any test. Reverting the surfacing fix to the old swallow (return error → return nil, or `_ = apply(...)`) keeps the ENTIRE suite green — the 'surface the error, don't swallow' fix, which is the commit's own headline, is a false-green.",
   "fix_teeth_sketch": "Add a Mac helper-level teeth that forces setAttributes to throw deterministically: create the DB file (and a -wal file so it enters the loop), then chflags(path, UF_IMMUTABLE) on the DB (UF_IMMUTABLE blocks metadata changes → setAttributes(.protectionKey) throws EPERM). Assert `apply(toDatabaseAt:)` returns a NON-nil error (XCTAssertNotNil) — proving the first error is RETURNED, not swallowed. In defer, clear the flag (chflags(path,0)) before cleanup. Adversarial reversal: change the helper's `catch { return error }` to `return nil` → this test reds while every existing test stays green, closing the surfacing false-green. (Store-level fileProtectionError surfacing is harder to force via FS because an immutable DB fails sqlite open first; the SessionPersist hook arm is MLX-gated and heavier — the helper-level teeth is the clean, high-value win. Optional lower-value add: assert the -shm sidecar also flips, which no current test does.)"
  },
  "false_green_risk": {
   "exists": true,
   "description": "Nuance: the EXISTING readback flip test is NOT a false-green — the authors correctly pre-set .complete and assert the flip, so it survives adversarial reversal of the setAttributes call. The remaining false-green is on the SURFACING fix (x-sov #6): no test forces apply() to return non-nil, so reverting the helper's error-return to a swallow (or the store's assignment to `_ = apply()`) keeps the whole suite green. The 'don't try?-swallow the protection error' half of commit 6c600c0c1 has no adversarial reversal guarding it."
  },
  "plumbing_only_test": {
   "exists": false,
   "description": "No device A/B or on-device probe exists for this item at all (DeviceTestApp has no file-protection / at-rest / fileProtectionError probe), so there is no plumbing-only device assertion to flag. At-rest verification is asserted only in prose ('iOS-device-verified') with no committed device harness."
  },
  "recommended_action": "Mac-side fix available now: add the helper-level surfacing teeth described above (chflags UF_IMMUTABLE → assert apply returns non-nil; adversarial reversal = swallow reds). This closes the x-sov #6 false-green entirely on macOS. After that, the ONLY honest device-only remainder is the iOS pre-first-unlock at-rest kernel unreadability of the memory-atom DB + sidecars, which needs a real locked device."
 },
 "9": {
  "id": 9,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.78,
  "file": "Sources/BASMLXAdapter/MLXOrganAdapter.swift:1696",
  "mac_layer_verified": "The pure hysteresis + action-subsumption layer of the pressure ladder is fully Mac-verified: BASPressureLadderTests.swift covers rung ordering/thresholds/latch/rearm/collapse and reclaimActions(forDeepest:) subsumption, and testMacWiringIsInert confirms _pressureCheck is a no-op off-iOS (headroom probe nil). The park machinery itself is code-verified: _parkPending (MLXOrganAdapter.swift:1696) removes the victim from `sessions` synchronously but retains it in `pendingSpill` (strong ref via _PendingSpill), and spawns the _persist writer on a DETACHED Task — so it is non-blocking and the free is deferred (the 'virtual reclaim' is real by construction). _persist (MLXOrganAdapter+SessionPersist.swift:46) → BASSessionKVStore.save → MLX.save(arrays:) forces evaluation/materialization of the KV slices, and the park path always passes quantizeKV:false (fp16 = largest transient). The clear-epoch suppression seam (_snapshotSeat) is already Mac-tested with a fake writer in BASSnapshotClearRaceTests. So: the WHEN/lifetime/ordering is code-established; only the byte-level memory outcome is not.",
  "device_only_layer": "The irreducible measurement: whether the async spill write actually frees the KV bytes from the MLX buffer pool once it lands, and — the crux of LOW-16 — whether the fp16 materialization transient inside MLX.save momentarily RAISES the process footprint at <15% headroom (net-worsening rung-1 short-term). This needs a real Qwen3.5-4B KV cache + os_proc_available_memory() (nil off-iOS, MTPSpec.swift:331) on device to observe the allocation curve across the park.",
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "description": "The park path unconditionally persists fp16 (quantizeKV:false) in BOTH _parkPending/_completePendingSpill and the dream-loop snapshotWarmSeats — i.e. the LARGEST possible transient materialization is used exactly when headroom is scarcest (rung-1 fires below 15% cap). There is no quantize-under-pressure decision, which is the one Mac-side lever that bounds LOW-16's transient. Also, no Mac unit test asserts the park lifetime invariant (box remains in pendingSpill until the writer drains, proving reclaim is deferred not synchronous).",
   "fix_teeth_sketch": "Extract a pure static helper e.g. `_spillQuantizeUnderPressure(headroomFrac: Double?) -> Bool` returning true when headroomFrac != nil && headroomFrac <= dropBelow (0.10) — halving the transient exactly on the pressure-driven park while keeping fp16 exactness on the idle dream-loop path (headroomFrac nil). Wire _parkPending's _persist call through it. Teeth (Mac, no model, like BASPressureLadderTests): assert true at 0.08, false at 0.30, false at nil; ADVERSARIAL REVERSAL: flip the comparator and the 0.08 case must red. Optional second teeth: a fake-writer seam over _completePendingSpill (mirroring _snapshotSeat) asserting pendingSpill[key] stays populated until the write returns, then is cleared — documenting the virtual-reclaim lifetime deterministically."
  },
  "false_green_risk": {
   "exists": false
  },
  "plumbing_only_test": {
   "exists": true,
   "description": "BASPressureLadderDeviceTests.testLadderFiresUnderSyntheticPressure is PLUMBING-ONLY w.r.t. both rung-1's KV-preservation claim and LOW-16. (1) It asserts rung-1 'survival via warm restore' only as `XCTAssertFalse(recallB.isEmpty)` — non-empty output — and it probes seatB, which NEVER received the codeword, so a cold-started (KV-lost) seat answers identically; the probe cannot distinguish warm-restore from cold-start and never checks the KEPT/parked seat that actually carries distinguishing state. (2) It asserts the ladder fired (telemetry contains 1/2) and the process survived, but makes NO memory assertion around the park — it never verifies rung-1 net-REDUCED footprint, which is the exact LOW-16 claim. So the LOW-16 net-reclaim/transient-allocation behavior is untested even on device."
  },
  "recommended_action": "Not fully device-only. Mac-side now: add the pure `_spillQuantizeUnderPressure(headroomFrac:)` decision helper + Mac unit teeth with adversarial reversal (small), routing the rung-1 pressure-park through Q4 to bound LOW-16's transient while leaving the idle dream-loop path fp16-exact — this closes the one Mac-verifiable sub-layer. Device run still required to confirm the actual byte outcome: extend BASPressureLadderDeviceTests to sample os_proc_available_memory immediately before the park and after the writer drains, asserting a monotone non-worsening (or bounded transient) across rung-1 — and fix its plumbing-only recall probe to park the codeword-bearing seat and assert content recall, not just non-empty output.",
  "summary": "LOW-16 is real and untracked but only PARTLY device-gated. The pure ladder + action subsumption is fully Mac-tested; the park path's virtual-reclaim (box retained in pendingSpill, write on a detached Task) and fp16 materialization are code-established. The genuinely device-only piece is the MLX byte outcome (does the fp16 transient net-worsen footprint at <15% headroom, does the write free the KV). Unclosed Mac sub-layer EXISTS: the park always persists fp16 (largest transient) with no quantize-under-pressure decision — a small pure helper + adversarially-reversible unit teeth is available now. The device cert is plumbing-only for LOW-16: it asserts rung fired + process survived and a mis-targeted non-empty recall probe, never that rung-1 reduced memory."
 },
 "13": {
  "id": 13,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.83,
  "file": "DeviceTestApp/Sources/App/BASQuantABProbe.swift:137",
  "summary": "30 *Probe.swift files (~36 sub-probes once env-gated sub-runs inside V12/CoreAI files are counted) under DeviceTestApp/Sources/App. NONE use XCTAssert/#expect — every probe writes a computed verdict STRING to a Documents ProbeFileLog (BASProbeCommon.swift) that a human pulls and reads, so there is no on-device test to be classically false-green; the risk is elsewhere. Two layers emerge. CLOSED Mac layer: the semantic/acceptance/honesty verdict math is already extracted into library pure types WITH host tests — BASAcceptanceBlockReducer (BASAcceptanceBlockReducerTests), BASModelHonestySignal (BASModelHonestySignalTests), BASAdjudicatorABEval (BASAdjudicatorABEvalTests), BASNLIReconcile (BASNLIReconcileTests); those probes (SpecAcceptance, V12 ADJ_AB/SEMANTIC/NLI sub-probes) only produce a raw bool-agreement array or reply string on device and hand it to an already-host-tested reducer. UNCLOSED Mac layer: the PERF-A/B promotion verdict — the drift-refusing bracket comparison 'challenger wins ONLY if mean < bracket minus driftBand' — is computed INLINE in the device probe and is NOT extracted, NOT host-tested. It appears at BASQuantABProbe.swift:137 (SPEED) and again at :118 (KVQUANT), with analogous inline bracket/drift/speedup thresholds in BASSpecSpeedupProbe and the take-5 endurance verdict. grep confirms NO Tests/ or Sources/ file defines a BracketVerdict/driftBand type — the actual promotion decision has zero Mac coverage.",
  "mac_layer_verified": "Verdict-computation for the semantic/acceptance/honesty probe class is extracted into BASOrgan/BASAcceptanceBlockReducer.swift plus sibling library types and covered by host tests (BASAcceptanceBlockReducerTests, BASModelHonestySignalTests, BASAdjudicatorABEvalTests, BASNLIReconcileTests, MLXV12HostDryRunTests). The ProbeFileLog persistence plumbing and the model-purge allowlist logic are also pure/Mac-side. For those probes the Mac verdict layer is genuinely closed; only the raw MLX forward pass is device-bound.",
  "device_only_layer": "Irreducibly device-only: real MLX tok/s and mean_ms under an A19 memory system; the thermal drift magnitude itself (pre vs post bracket slide); jetsam survival of a 4B-GDN+MambaCache in 8GB; and the actual model output bytes/quality that a human reads in QuantAB QUALITY-PAIRS. Those cannot be reproduced on a Mac and legitimately need the phone.",
  "plumbing_only_test": {
   "exists": true,
   "description": "The device-side judgment for several probes is plumbing-only. BASQuantABProbe QUALITY gate emits (4bit,3bit) body PAIRS with NO automated verdict at all — degradation is 100% human-read, so a real quality regression emits identically and 'passes'. Completion lines like 'PROBE DONE — SURVIVED (no jetsam)' and 'spec-alpha DONE ... ran end-to-end' assert only it-ran/rows-emitted. DecodePlannerAB logs byte_equal per row but nothing requires all_byte_equal=YES — a NO row is merely printed. The real promotion verdict (SPEED-WIN, BUILD/MARGINAL/FAIL) is computed but never asserted anywhere, on device or Mac."
  },
  "false_green_risk": {
   "exists": false,
   "description": "No probe contains an XCTAssert/#expect, so there is no existing code-path test that could pass for the wrong reason. The related but distinct hazard is that the inline promotion verdict (drift-refusing bracket comparison) is UNGUARDED by any test — a miscoding (driftMs vs driftPct, sign, <= vs <) would silently misclassify a regression as SPEED-WIN and the human trusts the string. That is an unclosed-gap, not a false-green."
  },
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "description": "The drift-refusing bracket promotion verdict is the actual gate that turns raw ms into a promote/decline decision, yet it is inlined in device probes (BASQuantABProbe:137 and :118; BASSpecSpeedupProbe; take-5 endurance) and has zero host tests. Same class as id19: a Mac-verifiable decision layer buried inside a device probe.",
   "fix_teeth_sketch": "Extract a pure 'BASBracketVerdict.classify(preMs, challengerMs, postMs) -> .win/.inconclusiveWithinDrift' into BASOrgan (bracket = (pre+post)/2, driftBand = abs(post-pre), win iff challenger>0 && challenger < bracket - driftBand). Refactor the two QuantAB sites + SpecSpeedup to call it (no behavior change). Add BASBracketVerdictTests with ADVERSARIAL reversal cases: (a) genuine win pre=100,post=102,ch=80 -> .win; (b) regression-within-drift pre=100,post=140,ch=110 -> .inconclusive (must NOT be win — this is the case a naive `ch<bracket` check false-greens); (c) drift larger than delta pre=100,post=160,ch=125 -> .inconclusive; (d) challenger==0 (load fail) -> .inconclusive. This closes the promotion-logic layer on Mac while leaving the raw ms measurement device-only."
  },
  "recommended_action": "Mac-side work IS available now: extract the inline drift-refusing bracket promotion verdict (BASQuantABProbe:137 and :118, plus BASSpecSpeedupProbe / take-5) into a pure BASBracketVerdict library function and add an adversarial host test (regression-within-drift must NOT read as SPEED-WIN). Secondary: the QuantAB QUALITY gate and DecodePlannerAB byte-equal are the genuinely device+human-only remainder — leave device-gated. Do NOT close item 13 as device-only; the promotion-decision layer is Mac-testable and currently uncovered."
 },
 "14": {
  "id": 14,
  "located": true,
  "genuinely_device_only": false,
  "confidence": 0.9,
  "file": "Sources/BASSovereign/BASSovereignAuditLedger.swift:826",
  "summary": "LOW-8 = the \"dual SHA impl\" coupling in BASSovereignAuditLedger: append() seals selfHash via sealHash()->routed Rust sha2 (hashViaAutoRouter -> bas_ranker_ledger_seal), while the verify side recomputes SHA. The mislabel to fix: this is NOT device-gated. The whole coupling runs on macOS (the FFI is #if os(iOS)||os(macOS), and ch741 test line 29 asserts choice==.rustLedgerSeal PASSES on Mac, proving the real Rust path executes under `swift test`). The gaps json already says macFixable:true; the operator-decisions doc grouping it under \"device-gated (need hardware)\" is over-conservative and wrong. Real unclosed Mac gap: the LOW-8 remediation was applied to verifyChainIntegrity (line 671 uses sealHash) but MISSED auditChainFull() — line 826 still recomputes `hash(canonical)` (CryptoKit) instead of `sealHash(canonical)`. auditChainFull is a second independent verify entrypoint (BR-013 non-throwing sentinel); under the production default useRoutedSeal=true, if routed and plain SHA ever diverged it would false-quarantine EVERY entry — the exact failure LOW-8 warns about — while verifyChainIntegrity stays robust. The sealHash docstring (1340-1342) itself claims routing is unified across \"append + verifyChainIntegrity\" and silently omits auditChainFull.",
  "mac_layer_verified": "The Rust-sha2 vs CryptoKit SHA256 byte-equality is genuinely Mac-verified (not device-gated): ch741 SovereignSealByteEquality asserts choice==.rustLedgerSeal + golden vectors (proves the Rust FFI actually runs on Mac, not a silent CryptoKit fallback); ch716 sweeps 50 varied entry shapes + a 10-step chain + NIST anchor; QINAOGatesBatch3 (line 880) round-trips a routed-sealed chain through verifyChainIntegrity() and (916-923) probes primitive equality. verifyChainIntegrity's use of sealHash (line 671) is the applied half of the fix.",
  "device_only_layer": "None that is irreducible. The only residue a real device adds is confirming the iOS-arm64-built XCFramework's bas_ranker_ledger_seal emits the same bytes as macOS-arm64 — but SHA256 is FIPS 180-4 bit-exact and both targets compile the same sha2 source, so macOS parity is a near-total proxy. No at-rest/thermal/energy/crash behavior is involved.",
  "unclosed_mac_gap": {
   "exists": true,
   "effort": "small",
   "fix_teeth_sketch": "FIX: change auditChainFull() line 826 `if appended.selfHash != hash(canonical)` -> `!= sealHash(canonical)`, unifying it with append() and verifyChainIntegrity() (sealHash is an internal method on the same actor, immutable-safe). TEETH (to avoid a false-green — a plain routed-seal-then-assert-auditChainFull().isClean test is false-green because Rust==CryptoKit today, so it passes whether 826 uses hash() or sealHash()): add a test-only injectable routed-hash seam (e.g. static nonisolated(unsafe) var routedHashOverride:((Data)->String)? consulted by hashViaAutoRouter/sealHash), seal a chain with an override that returns a DELIBERATELY-diverged-from-CryptoKit digest, then assert BOTH verifyChainIntegrity() does not throw AND auditChainFull().isClean==true. With line 826 on hash() the sentinel reds (.selfHashMismatch on every entry); with sealHash() it greens. Adversarial reversal is built in."
  },
  "false_green_risk": {
   "exists": true,
   "description": "Two spots. (1) ch716 AuditLedgerByteEqualityTests does NOT assert r.choice==.rustLedgerSeal, so in isolation it could compare CryptoKit-to-CryptoKit if the Rust FFI ever fell back to the Swift path — currently compensated by ch741 line 29 + batch3 golden in the same target, so not live, but ch716 alone lacks the guard. (2) More importantly, any positive test that seals routed then asserts auditChainFull().isClean==true is a false-green for the auditChainFull gap: because Rust sha2 == CryptoKit today, it passes identically whether line 826 uses hash() or sealHash(), giving false confidence the two verify paths are unified when they are not."
  },
  "plumbing_only_test": {
   "exists": false,
   "description": "No device A/B plumbing-only test is involved here — the item has no device test; the concern is the false-green positive-clean path described above."
  },
  "recommended_action": "Mac-side fix I can do now: change BASSovereignAuditLedger.swift:826 hash(canonical) -> sealHash(canonical) so auditChainFull() routes identically to append() + verifyChainIntegrity(), and update the sealHash docstring (1340-1342) to include auditChainFull. Add the injectable-seam adversarial teeth (asserts both verify entrypoints track a deliberately-diverged seal) so the fix survives reversal. Also correct the ledger classification: this item is macFixable, not device-gated. Optional hardening: add r.choice==.rustLedgerSeal assertion to ch716 to remove its latent false-green."
 }
}
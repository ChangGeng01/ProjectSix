{
 "generated": "2026-07-09",
 "method": "independent re-sweep of all 22 mega-audit evidence files (22-agent high-effort) cross-checked vs the 94-item accounting + git + current code",
 "total_findings_across_files": 285,
 "lacking_verified_closure": 113,
 "by_severity": {
  "MED": 28,
  "LOW": 79,
  "HIGH": 4,
  "CRITICAL": 2
 },
 "by_status": {
  "ACCOUNTED_OPEN": 22,
  "NOT_IN_ACCOUNTING": 83,
  "FALSE_CLOSE": 4,
  "CLAIMED_CLOSED_BUT_UNVERIFIED": 4
 },
 "mac_fixable": 99,
 "gaps": [
  {
   "id": "C1 (HF token in pushed history)",
   "severity": "CRITICAL",
   "defect": "A real HuggingFace token was committed and pushed to origin/decode-planner (docs/Recovery/*.jsonl + FULL_CONVERSATION md).",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Not present in this MED accounting file (grep: absent). Per MEMORY.md the operator attests to out-of-band rotation on 07-09 (credential risk discharged), but that is not verifiable from the repo and the live token STRING remains in pushed history (commit 5f9202c5a still an ancestor of HEAD and on origin). History cleansing is operator-gated under the deletion-aversion rule → the finding is not verifiably closed within the repository.",
   "macFixable": false,
   "subsystem": "synthesis"
  },
  {
   "id": "x-sovereignty #1 (CRITICAL)",
   "severity": "CRITICAL",
   "defect": "Real HuggingFace token hf_kEwD…myTw committed and pushed to the private GitHub remote (commit 5f9202c5a + LFS), ~20 occurrences across two docs/Recovery files.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Absent from the 94-item MED ledger (attest.total=94; the ledger is MED-scoped and lists no CRITICAL). Partial remediation exists and is real: dd4ecf556 redacts the token from the WORKING TREE (grep 'hf_kEwD' on the working tree now returns rc=1, no matches) and c0f4f5382 adds a staged secret-scan pre-commit guard; dd4ecf556's message attests the operator REVOKED the token. BUT the exposure is in-history: `git log -S 'hf_kEwD' --all` still returns the introducing blob 5f9202c5a (plus LFS), and dd4ecf556 explicitly states 'History not rewritten' (filter-repo deletion-class path deferred to operator). So the in-repo exposure-in-history is NOT verifiably closed; the live-credential discharge rests solely on an out-of-band operator revocation I cannot verify from the repo. The broader mega-audit report b6b31a3b1 labels it 'CRITICAL (token, closed)', but that closure is redaction+rotation, not removal from pushed history.",
   "macFixable": false,
   "subsystem": "x-sovereignty"
  },
  {
   "id": "hostkit-rest HIGH-2",
   "severity": "HIGH",
   "defect": "pipeline .all-tier builds BASAgentWatcherObservation with critic:nil / hostAlignment:nil hardcoded, scout/risk empty placeholders, and RAW memory/sovereignSentinel/evolutionShadow instead of the effective* filtered inputs → axis/hostDrift watchers permanently blind, and memoryPollutionWatcher fires on a seat that was filtered out (false L14 audit evidence)",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "The 94-item ledger's unaccounted_items lists only hostkit-rest MED-1/MED-4/MED-5; HIGH-1 was folded into H18 (8a5e5d17b) but HIGH-2 is absent from every §8 HIGH table, §3 theme, and the unaccounted list. Only accounting hit for 'AgentFabricHostPipeline' is MED-1's FullTurnAdapter line, not the watcher block. git log post-2026-07-07 for BASAgentFabricHostPipeline.swift is EMPTY; the 3 git 'HIGH-2' hits are unrelated auditors' findings (ch1044 sovereign nonce, HostAlignment adapter chapters). Current code Sources/BASHostKit/BASAgentFabricHostPipeline.swift:512-531 still shows critic:nil, hostAlignment:nil and memory:memoryInput/sovereignSentinel:sovereignSentinelInput/evolutionShadow:evolutionShadowInput — while the effectiveMemory/effectiveSovereignInput/effectiveEvolutionInput computed at :351-385 are only threaded into the MAIN watcher path at :394-398, never the .all block. Defect fully live.",
   "macFixable": true,
   "subsystem": "hostkit-rest",
   "resolved": "7321781f1 (effective inputs + build critic/hostAlignment)"
  },
  {
   "id": "HIGH-1",
   "severity": "HIGH",
   "defect": "guardPaths schema field is written with directly OPPOSITE semantics by two production producers: BASAgentFabricAdapters.candidateFrontierProjection sets guardPaths = candidates with reversibility < 0.3 (danger / guard-needed band), while EBrainNeuralMaterializationCore sets guardPaths = candidates with reversibility >= 0.7 OR conservative-lexicon match (safe-retreat band) — both feed the same BASCandidateFrontier.guardPaths consumed by L9 planning. reversiblePaths thresholds also drift (adapter >=0.7 vs neural >=0.6).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "grep of MED_55_ITEM_ACCOUNTING_2026-07-09.json for 'orchestration HIGH-1' / 'guardPaths' / 'reversal' returns ZERO entries — the only HIGH-1 rows are unrelated (x-concurrency HIGH-1, hostkit-rest HIGH-1, ch1044 sovereign-nonce). No git commit references guardPaths semantics. Current code confirms both producers live: BASAgentFabricAdapters.swift:362-363 (guardPaths = reversibility < 0.3) and EBrainNeuralMaterializationCore.swift:328-333 (guardPaths = reversibility >= 0.7 || guardLexicon). A HIGH-severity semantic-inversion sitting on the frontier/retreat-selection path was silently dropped from the accounting entirely.",
   "macFixable": true,
   "subsystem": "orchestration",
   "resolved": "1a7745806 (guardPaths inversion reconciled to safe-retreat semantic)"
  },
  {
   "id": "runtimecore-a #1 (HIGH)",
   "severity": "HIGH",
   "defect": "bas_wallclock_nanos claims a sleep-INCLUSIVE clock but is implemented with mach_absolute_time(), which does NOT advance during system sleep — same semantics as the sleep-EXCLUDED BASMonotonicNanos; the truly-continuous source (mach_continuous_time / CLOCK_MONOTONIC) is never used.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "The 94-item accounting lists ONLY runtimecore-a #2 and #3; #1 has no entry (grep 'runtimecore-a' → 2 hits; JSON has no wallclock/mach/sleep-clock item). The only wallclock commit 85c1fb4a3 (2026-05-21) is the ORIGIN feature, PREDATES the audit (Jul 7). No post-audit commit touches bas_monotonic_nanos.c. Current bas_monotonic_nanos.c:388-427 still calls mach_absolute_time() in bas_wallclock_nanos. Dormant (zero production consumers) but exported, versioned, test-backed ABI that silently fails once adopted — audit rated it HIGH. Includes the '附带' sub-defect: V1 epoch-based vs V2 boot-based timebase mismatch with a doc calling V2 'strictly-better'.",
   "macFixable": true,
   "subsystem": "runtimecore-a",
   "resolved": "fbda118cf (mach_continuous_time; teeth device-gated)"
  },
  {
   "id": "runtimecore-a #1 (wallclock)",
   "severity": "HIGH",
   "defect": "bas_wallclock_nanos sleep-semantics reversed: comment claims it includes sleep intervals but the underlying clock stops during sleep, so a stale post-sleep attestation is treated as fresh (HIGH-dormant).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Grep of MED_55_ITEM_ACCOUNTING shows only 'runtimecore-a #2' and '#3' as line items — #1 is absent entirely. `git log --all | grep wallclock` returns only feature commits (85c1fb4a3 'sleep-inclusive clock' dated 2026-05-21, predating the 07-07 audit; a033a5817; 876ae7e8c) — no post-audit fix. Sibling M-e findings #2/#3 (5f36be54a) and #4 (259b85288) all got fixes; #1 was silently dropped from both the theme close and the per-item ledger.",
   "macFixable": true,
   "subsystem": "synthesis",
   "resolved": "fbda118cf (mach_continuous_time; teeth device-gated)"
  },
  {
   "id": "MED-9",
   "severity": "MED",
   "defect": "BASQwen35MTPProbe.runSustained re-inits the spec decoder every 96-token generation (BASQwen35MTPProbe.swift:66-70), re-quantizing ~300MB/gen and artificially inflating thermal drift → false 'thermal wall' negative; the cert arm already fixed this exact self-heating bug but the sustained arm was left as-is.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Accounting JSON lists it in device_gated_reexam_2026_07_09.not_mac_fixable as 'devicetestapp MED-9: device-only (MLX-coupled probe)', and in tail_campaign device_runtime_gated_remaining. It is HONESTLY tracked as an open device-only residual — no code fix. git log --all shows NO commit referencing devicetestapp MED-9 or a runSustained decoder-reuse fix. Current BASQwen35MTPProbe.swift:66-70 still re-inits the decoder per generation. Not verifiably closed; tracked-open.",
   "macFixable": false,
   "subsystem": "devicetestapp"
  },
  {
   "id": "hostkit-rest MED-2 (count-inflation half)",
   "severity": "MED",
   "defect": "submitWithForbiddenZoneGate / ingestTicketsWithForbiddenZoneGate unconditionally return .proposed and count already-.rejected tickets as accepted → inflated accepted metrics; the 'audit-promise is fake' half was closed only by rewriting the docstring to admit nothing is recorded (comment-lie/doc-only), so an auditor grepping lifecycle.zoneGate: at submit still gets zero hits",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Accounting item hostkit-rest MED-2 is marked disposition:closed via 42ec9cab3, but its own detail states 'Residual: the unconditional-.proposed count-inflation sub-defect is the LOW-2-class issue and was not separately patched.' Commit 42ec9cab3 only added approveForDistillationWithForbiddenZoneGate (that is MED-3) and corrected the submit docstring. Current code Sources/BASHostKit/BASUpdateTicketLifecycleForbiddenZoneGate.swift:86-96 still discards the gate result (_ = ...gate(...)) and the count-inflation path is unpatched.",
   "macFixable": true,
   "subsystem": "hostkit-rest"
  },
  {
   "id": "memory-b F3",
   "severity": "MED",
   "defect": "BASEventSourcedMemoryAtomStore.admit() content-cache pollution: on a conflicting re-admit (same UUID, lower confidence, different content) the M942 reducer keeps the winning metadata but admit() unconditionally overwrites contentCache with the loser content, so atom(forID:) returns a winner-metadata + loser-content hybrid atom.",
   "status": "FALSE_CLOSE",
   "evidence": "Accounting item 'memory-b F3' disposition=\"closed\" but has NO remediation field; its own detail admits the second (behavioral) clause is 'NOT fixed ... Residual behavioral defect effectively dropped.' The single commit eb7d268ec is docs-only ('docs(memory): correct admit()'s lying return-value comment', 'no behavior change', 9 comment insertions). Current code confirms the defect is live: Sources/BASMemory/BASEventSourcedMemoryAtomStore.swift:320 still `contentCache[atom.id.uuidString] = atom.content` unconditionally when appended==true, and hydrateContent (line ~172) sources content from contentCache. No behavioral commit exists.",
   "macFixable": true,
   "subsystem": "memory-b"
  },
  {
   "id": "MED-8",
   "severity": "MED",
   "defect": "Cold-start concurrent MTP turns double-build the ~300MB decoder (priorBox captured on actor before container.perform), transient ~600MB overlap approaching the 3376MB uncapped ceiling + chainEmaL loss.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Accounting item 'mlx-adapter-core MED-8' disposition='deferred'; remediation='DEFERRED (device follow-up)' — the double-build lives inside container.perform(nonSendable:) and deduping requires build-vs-decode restructuring with only device-verifiable payoff. Only the single-flight primitive (_loadOnce, from MED-9 556ff5db8) shipped; the decoder-construction dedup itself is NOT done. No commit touches MTPSpec priorBox capture at :51/140. Genuinely open/deferred, not closed.",
   "macFixable": false,
   "subsystem": "mlx-adapter-core"
  },
  {
   "id": "mlx-decode MED-1",
   "severity": "MED",
   "defect": "MTP stream KV bound maxSeq=2048 not enforced on the SAMPLING lane: generateSpecSampling -> mtpForward writes mtpK[pos]=k / mtpV[pos]=v (BASQwen35MTPSpecDecoder.swift:154-155) with pos=hLastPos=trunkLen-1 and no clamp against maxSeq=2048; a >2048 prompt/transcript overruns the buffer and silently enters the net-negative speedup region.",
   "status": "FALSE_CLOSE",
   "evidence": "Accounting marks mlx-decode MED-1 'closed' via commit b81490202. `git show --stat b81490202` touches ONLY BASQwen35MTPSpecDecoder+FusedChain.swift (added clampedChainWidth for the FUSED chain draft width) + a test. The evidence file's MED-1 explicitly names TWO write points and states 'fused 与 sampling 两条生产车道都中'; the second, generateSpecSampling's mtpForward at BASQwen35MTPSpecDecoder.swift:154 ('sampling 车道 generateSpecSampling 同样无任何钳位'), is NOT touched by the commit and current code still has the bare `mtpK[pos] = k` with hLastPos = prompt.count-1 / trunkLen-1 growing unbounded (no `pos < maxSeq` guard anywhere in the file; grep for maxSeq/clamp shows only buffer allocation, no write-time clamp). The shipped temp>0 rejection-sampling lane (~1.05x) thus retains the defect. Fused half fixed, sampling half live => not verifiably fully closed.",
   "macFixable": true,
   "subsystem": "mlx-decode",
   "resolved": "6e8e75655 (all 5 mtpForward sites bounded; my prior fix was partial)"
  },
  {
   "id": "MED-2",
   "severity": "MED",
   "defect": "Rust FFI dominance-order index applied with precondition(i>=0 && i<count) that ABORTS the whole process on an out-of-bounds index instead of using the Swift fallback sort 8 lines below, and only checks bounds — NOT duplicate indices (Rust returning a repeated index silently produces a dominanceOrder with a duplicated/dropped candidateID).",
   "status": "FALSE_CLOSE",
   "evidence": "Accounting marks orchestration MED-2 'closed' via commit 13a7e5b66. But `git show --stat 13a7e5b66` touches ONLY Sources/BASHostKit/BASMLMemoryService.swift (+ a test) — a PARALLEL call site of dreamLoopDominanceOrderDouble. The auditor's EXACT cited surface, EBrainNeuralMaterializationCore.swift:306-310, is untouched: current code at EBrainNeuralMaterializationCore.swift lines ~305-313 still has the crashing `precondition(i >= 0 && i < thoughtFrame.candidates.count, ...)` on the Rust indices with NO duplicate-index check, fallback still at ~318-323. `git log -S dreamLoopDominanceOrderDouble` on that file shows only pre-audit chapter e786018bc. The fix repaired a different file; the cited defect remains live.",
   "macFixable": true,
   "subsystem": "orchestration"
  },
  {
   "id": "MED-4",
   "severity": "MED",
   "defect": "recordEvent(_:streamingTo:) commits BOTH the traceLog and durable eventLog, THEN calls sink.receive(); if the sink throws after the successful double-write, the return tuple (carrying traceSeq) is lost, so a caller retry allocates a NEW traceSeq → new eventID → the event-log idempotency key changes (wasNew=true) and both logs gain a true un-dedupable duplicate audit record.",
   "status": "CLAIMED_CLOSED_BUT_UNVERIFIED",
   "evidence": "Accounting marks orchestration MED-4 'closed' via 524f1ffa4, but that commit only touched the BASE BASAgentTraceLogEventLogBridge.recordEvent (wraps eventLog.append failure in PartialWriteError). The auditor's cited surface BASAgentTraceStreamingSink.swift:155-170 is still at scaffold commit 38aa3afe1 (git log on the file). Current code (BASAgentTraceStreamingSink.swift:155-171): `let result = try await recordEvent(event)` [both logs commit] ... `try await sink.receive(stamped, ...)` [throws → result with traceSeq discarded] `return result`. The base PartialWriteError fix does NOT cover the sink-throws-AFTER-commit case — the retry-duplicate defect is unremediated. Accounting itself admits 'the exact cited surface was not directly touched'.",
   "macFixable": true,
   "subsystem": "orchestration"
  },
  {
   "id": "organ-eval MED-2",
   "severity": "MED",
   "defect": "AB judge fidelity anchor is silently bypassable: when parser rows all carry nil tokHash and no external count is passed, fidelityMismatches==0, so a log containing FIDELITY-FAIL lines still yields report overall:.pass (byte anchor is virtual).",
   "status": "CLAIMED_CLOSED_BUT_UNVERIFIED",
   "evidence": "Accounting item 'organ-eval MED-2' disposition=closed, remediation=commit f8f382c6c. The commit is a REAL code change to Sources/BASEvaluation/BASABProtocol.swift, but it only ADDS an observability field `fidelityVerifiable`; it does NOT fail-close the verdict. Verified current code: BASABProtocol.swift:154 `if spec.fidelityAnchorRequired, mismatches > 0` — with all-nil tokHash, mismatches==0, this branch is skipped and judge falls through to :246-248 `overall = .pass`. The original behavioral defect (caller forgets fid -> FIDELITY-FAIL yet overall:.pass) persists; the commit body itself states 'a stricter verdict-level fail-closed is DEFERRED'. A consumer that checks `overall == .pass` is still fooled. Partial close mislabeled as closed.",
   "macFixable": true,
   "subsystem": "organ-eval"
  },
  {
   "id": "organ-eval MED-5",
   "severity": "MED",
   "defect": "Verification pipeline has TWO clauses: (1) gated-skip returns empty perStage indistinguishable from all-pass; (2) failure labels appended in Swift dictionary-random order, violating the file's pinned M892 replay-determinism contract.",
   "status": "CLAIMED_CLOSED_BUT_UNVERIFIED",
   "evidence": "Accounting item 'organ-eval MED-5' disposition=closed, remediation=commit a7c5e093e. That commit closes ONLY clause 1 (adds `gatedSkip: Bool` so a skipped verify != a passed one). Clause 2 is UNTOUCHED: current Sources/BASOrgan/BASLLMVerifierPipeline.swift:556 still `for (stage, outcome) in report.perStage` (nondeterministic dictionary iteration), appending `verifier-stage-failed:<stage>` in random order. The evidence's prescribed fix (`for stage in BASLLMVerifierStage.allCases`) was not applied — no allCases/sorted iteration exists in the file. Serialized feedback remains non-reproducible cross-process. Partial close mislabeled as closed.",
   "macFixable": true,
   "subsystem": "organ-eval",
   "resolved": "882fa3422 (clause 2 deterministic failure-label order; my prior fix closed clause 1)"
  },
  {
   "id": "runtimecore-b MED-3 (evidence item #3)",
   "severity": "MED",
   "defect": "KG/EvalRun SQLite read paths fail-open: corrupt OR busy DB returns a silent empty result. The prescribed fix was to copy the event-log three-piece set (onSilentFailure hook + eventsOrThrow sibling + PRAGMA busy_timeout=5000).",
   "status": "CLAIMED_CLOSED_BUT_UNVERIFIED",
   "evidence": "Accounting marks MED-3 closed via af4b38b9f. That commit is real but only adds an OPEN-time PRAGMA integrity_check gate (BASSQLiteIntegrity) — it closes the on-disk structural-corruption half only. The busy/read-time half the finding explicitly named is untouched: current BASSQLiteKnowledgeGraphStorage.swift:250,361 still `(try? Self.fetchAllNodes/​fetchAllEdges(db:)) ?? []`, and grep for busy_timeout|onSilentFailure|eventsOrThrow across both KG and EvalRun stores returns NOTHING. Failure scenario A (WAL-checkpoint SQLITE_BUSY on allNodes() during preload → empty knowledge graph, indistinguishable from a fresh user) is still live, and busy_timeout is still unset so BUSY can occur. Partial close reported as full.",
   "macFixable": true,
   "subsystem": "runtimecore-b"
  },
  {
   "id": "memory-b F3 (2nd clause)",
   "severity": "MED",
   "defect": "admit() content-cache hybrid: on a conflicting re-admit of the same atom.id with different content, appended==true so contentCache[id] is overwritten with the loser content while the projection keeps winner metadata → atom(forID:) returns winner-metadata + loser-content hybrid.",
   "status": "FALSE_CLOSE",
   "evidence": "Accounting lists disposition=closed via commit eb7d268ec, but `git show --stat` shows that commit is DOCS-ONLY (subject 'docs(memory): correct admit()'s lying return-value comment', 9-line comment edit, body states 'no behavior change'). It fixes only the return-value comment-lie (1st clause). Current code BASEventSourcedMemoryAtomStore.swift:317-321 still does `if appended { contentCache[atom.id.uuidString] = atom.content }` unconditionally — the behavioral defect is live. The accounting's own detail admits 'the SECOND clause ... is NOT fixed by this commit ... Residual behavioral defect effectively dropped', yet the item is dispositioned closed and got NO backfill commit.",
   "macFixable": true,
   "subsystem": "synthesis"
  },
  {
   "id": "TrainingDataExporter 'Atomic rename' (§0 comment-lie)",
   "severity": "MED",
   "defect": "Comment claims 'Atomic rename' but code does removeItem(destination) then moveItem(temp,destination) — non-atomic; a crash/failure between the two loses the original file with the replacement not yet in place (data-loss window), and the comment misleads reviewers into trusting atomicity.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Named as a systemic comment-lie exemplar in synthesis §0 line 13. No med_id in the accounting; `git log --all | grep -i 'TrainingDataExporter|atomic rename'` returns no fix commit. Confirmed live in BASTrainingDataExporter.swift:616-627 ('// Atomic rename.' then `if fileExists { removeItem(url) }` followed by `moveItem(tempURL, url)`). Silently dropped by the §12 sweep.",
   "macFixable": true,
   "subsystem": "synthesis"
  },
  {
   "id": "hostkit-rest MED-2 (residual)",
   "severity": "MED",
   "defect": "submitWithForbiddenZoneGate unconditionally returns .proposed even for already-.rejected tickets, inflating the accepted count.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Accounting disposition=closed via 42ec9cab3, but its own detail states the commit fixed only the fake-audit-promise (docstring) half and that 'the unconditional-.proposed count-inflation sub-defect ... was not separately patched'. Behavioral sub-defect remains unremediated.",
   "macFixable": true,
   "subsystem": "synthesis"
  },
  {
   "id": "tests-arch ②b (residual)",
   "severity": "MED",
   "defect": "~64 (now 48) XCTAssertTrue(true) decision-pin permatrue assertions that can never fail.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Disposition=closed via 173860f42, but the detail explicitly says the assertCodable family was upgraded while 'the XCTAssertTrue(true) decision-pin sub-family was OUT of #18 scope and left in place by design (48 occurrences still in Tests today) — not individually dispositioned, so that half of the finding is effectively unremediated.'",
   "macFixable": true,
   "subsystem": "synthesis"
  },
  {
   "id": "x-test-integrity F5",
   "severity": "MED",
   "defect": "CRITICAL reading gates #20/#21/#26 backed by known-saturated probes (base=tuned=1.0); saturated backing not repointed to the discriminating reading_hard probe.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Disposition=partial. #26 unit-mismatch closed (a35c2254f) and orphan-wiring incidental via H23 (6c9f77c38), but the accounting states the remaining saturation-backing sub-part 'runs+merges but is gated by NO registry metric ... Operator/QINAO-campaign scope' — no code closure.",
   "macFixable": false,
   "subsystem": "synthesis"
  },
  {
   "id": "mlx-adapter-core MED-8",
   "severity": "MED",
   "defect": "Cold-start concurrent MTP turns double-build the decoder: transient ~600MB overlap + EMA loss.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "disposition=deferred; accounting: 'the cold-start double-build lives INSIDE a container.perform(nonSendable:) closure; deduping it safely requires restructuring the hottest decode path ... Deferred to avoid destabilizing production decode.' No fix commit.",
   "macFixable": false,
   "subsystem": "synthesis"
  },
  {
   "id": "devicetestapp MED-9",
   "severity": "MED",
   "defect": "BASQwen35MTPProbe.runSustained re-inits the spec decoder every generation → self-heating inflates thermal drift → false 'thermal wall' negative.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "disposition=device-only; cebea725a touched MTPProbe only for MED-2 (the sleeps); the per-gen decoder rebuild in runSustained is untouched. macFixableCore=false per workflow verdict. No fix commit.",
   "macFixable": false,
   "subsystem": "synthesis"
  },
  {
   "id": "tools-scripts MED (R2/R3 corpus)",
   "severity": "MED",
   "defect": "R2/R3 frozen corpus + sha pins live only in /tmp → a reboot/clean destroys the R4 freeze-clause executability.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "disposition=open; 'Blocker: experiment-domain (operator) ... not yet moved to Docs/evidence/. No fix commit.' Explicitly the one item the final tally leaves open.",
   "macFixable": false,
   "subsystem": "synthesis"
  },
  {
   "id": "memory-a F4 (VACUUM residual)",
   "severity": "MED",
   "defect": "Legacy free-page content bytes written before secure_delete=ON remain forensically recoverable until a one-time VACUUM.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Headline closed via f8ab74184 (secure_delete default-on across 18 stores), but accounting notes 'Residual (registered): one-time VACUUM of legacy free pages is operator-gated' — the pre-existing at-rest exposure is not cleared.",
   "macFixable": false,
   "subsystem": "synthesis"
  },
  {
   "id": "x-sovereignty #5 (at-rest residual)",
   "severity": "MED",
   "defect": "Memory-atom DB (highest-sensitivity at-rest point) lock-before-read encryption behavior unverified on hardware.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Data Protection code landed (6c600c0c1) but accounting states 'at-rest lock-before-read encryption behavior is device-gated (real-device only) ... hardware verification pending' — closure not verifiable without device.",
   "macFixable": false,
   "subsystem": "synthesis"
  },
  {
   "id": "memory-b F5",
   "severity": "MED",
   "defect": "Warm-cache incremental reduction could diverge from event-log order under actor reentrancy (state-machine consistency).",
   "status": "ACCOUNTED_OPEN",
   "evidence": "disposition=not-reproduced; NO code fix shipped. Closure rests solely on the auditor's own 60-way stress + structural no-op argument ('reversal did not red ⇒ did not ship a fix'). Not verifiably closed by any change — relies on self-refutation, unverified independently.",
   "macFixable": true,
   "subsystem": "synthesis"
  },
  {
   "id": "hostkit-spine F3",
   "severity": "MED",
   "defect": "gate-side vs audit-side projection 'byte-identical by construction' claimed false on a permit.mode-escalation turn (redline-8 suppression codes vs recorded humanAnchor tone).",
   "status": "ACCOUNTED_OPEN",
   "evidence": "disposition=not-reproduced; NO code fix. F3 was truncated out of the source report (§ note line 5) and never entered the ledger; closure rests only on the auditor's claim that permit.mode is invariant across the seam. Not independently verified.",
   "macFixable": true,
   "subsystem": "synthesis"
  },
  {
   "id": "orchestration MED-4 (cited surface)",
   "severity": "MED",
   "defect": "recordEvent(_:streamingTo:) variant where sink.receive() throws AFTER both logs commit → non-dedupable duplicate audit events.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "disposition=closed via 524f1ffa4, but the detail admits 'the auditor's exact cited surface — the recordEvent(_:streamingTo:) variant ... BASAgentTraceStreamingSink.swift, file still at scaffold commit 38aa3afe1 — was not directly touched, though it delegates to the now-fixed base recordEvent.' The specific throwing-sink surface is not exercised.",
   "macFixable": true,
   "subsystem": "synthesis"
  },
  {
   "id": "tests-arch ②b MED (XCTAssertTrue(true) decision-pin sub-family)",
   "severity": "MED",
   "defect": "64 (now 48) XCTAssertTrue(true,\"…\") tautological 'decision-pin' assertions that can never fail at runtime; no mechanism forces a code change violating the pinned decision to turn them red, and the library self-contradicts (BAS211MemoryAtomSingleSourceOfTruthPinTests.swift:31 bans the tautology while 48 others use it).",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Accounting item 'tests-arch ②b MED' has disposition=\"closed\", but its OWN detail admits: 'the XCTAssertTrue(true) decision-pin sub-family was OUT of #18 scope and left in place by design (48 occurrences still in Tests today, was 64) — not individually dispositioned, so that half of the finding is effectively unremediated.' Closing commit 173860f42 only touched the assertCodable family (verified: BASCodableRoundTripSupport.swift exists). Grep confirms 48 XCTAssertTrue(true occurrences remain across Tests/ today. Half the finding is open under a 'closed' label.",
   "macFixable": true,
   "subsystem": "tests-arch"
  },
  {
   "id": "tests-arch ④ MED (iOS source-gate coverage shrinkage residual)",
   "severity": "MED",
   "defect": "161 files entirely `#if !os(iOS)` source-gated → ~10% of suites absent from the iOS device bundle so 'device all-green' certifies a shrunk universe; and no per-file record of whether each file truly hits the SwiftTesting-iOS quirk or was blanket-gated.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Accounting item 'tests-arch ④ MED' disposition=\"closed\" via remediation commit 941b6dd75. git show confirms that commit adds only BASIOSGatedTestBundleCeilingTripwireTests.swift (98 insertions) — a growth tripwire that pins the count at 161 and surfaces un-gating as progress. It does NOT restore any coverage to the device bundle and adds no per-file quirk-attribution record. The commit message itself states the close is 'a regression tripwire, NOT un-gating (that needs per-file device-quirk work).' The coverage-shrinkage and per-file-verification halves of the finding remain open; only the silent-growth-tracking gap was closed.",
   "macFixable": true,
   "subsystem": "tests-arch"
  },
  {
   "id": "tools-scripts MED (R2/R3 frozen corpus + sha only in /tmp)",
   "severity": "MED",
   "defect": "b2_r2_pipeline.py freeze-clause corpus (b2_r2_corpus.jsonl, b2_r3_fresh.jsonl) and their .sha pins live only in /tmp/gdn_coreai; a reboot or 3-day periodic clean destroys the exclusion sets and makes R4 merge_r4 (requires both sets present + sha match) unexecutable.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Accounting item disposition='open', detail: 'Blocker: experiment-domain (operator). §11 line 501 lists it explicitly as residual ②b2 /tmp→Docs/evidence 冻结. Not yet moved. No fix commit.' remediation field empty. Current code b2_r2_pipeline.py still points CORPUS/FRESH at /tmp paths. Genuinely open, tracked, operator-gated.",
   "macFixable": true,
   "subsystem": "tools-scripts"
  },
  {
   "id": "x-concurrency MED-LOW-10",
   "severity": "MED",
   "defect": "BASMetalSyncBridge.swift:38-49 DispatchSemaphore.wait blocks the calling Swift-concurrency cooperative (actor-executor) thread up to timeoutMs waiting on its own spawned Task; under cooperative-pool saturation the awaited op never gets a thread -> 100% fallback + one burned thread x timeout per call (degradation, not deadlock).",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Accounting lists it with disposition=\"device-only\" and remediation text 'DEVICE-ONLY + LOW ... Left as a documented low-severity item' — i.e. explicitly NOT fixed. No post-audit commit touches BASMetalSyncBridge.swift (confirmed: file absent from all §12 remediation commits; accounting itself states 'No post-audit commit to the file'). Tracked-open, no code change shipped.",
   "macFixable": false,
   "subsystem": "x-concurrency"
  },
  {
   "id": "F5 (MED)",
   "severity": "MED",
   "defect": "CRITICAL reading gates #20/#21/#26 backed by known-saturated probes (base=tuned=1.0); only #26 unit-mismatch and orphan-wiring were fixed — the saturation-backing of #20/#21 (the discriminating qinao_reading_hard probe runs+merges but is gated by NO registry metric) is unremediated.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "Accounting entry 'x-test-integrity F5' disposition='partial'. Verified: #26 unit-mismatch CLOSED in a35c2254f (qinao_reading.py:12-21 novel_doc_read_score now returns mc/n fraction, no *100 — confirmed in current code); orphan-wiring CLOSED incidentally by H23 6c9f77c38 (qinao_merge.py KNOWN_SIDEFILES maps reading_hard). REMAINING per accounting remediation text: 'the discriminating qinao_reading_hard probe (reading_hard_followrate) runs+merges but is gated by NO registry metric — a QINAO design decision (new metric ID + threshold) to repoint the saturated #20/#21 backing. Operator/QINAO-campaign scope.' So the core F5 disease (a prior-reverting reader still passes the three CRITICAL reading-proof gates) is NOT closed.",
   "macFixable": true,
   "subsystem": "x-test-integrity"
  },
  {
   "id": "LOW-1",
   "severity": "LOW",
   "defect": "Time-cap-truncated endurance runs over-report iteration count: the FINAL line (BASEnduranceAppRunner.swift:2704-2708) and the terminal .completed status (:2779-2782) both emit totalIters = the CONFIG nominal (max(1,iters), :913), not the actual completed count. When maxRuntimeSec truncates at iter=k the loop honestly breaks with 'completed k-1 iters' (:1945-1949) but downstream per-iter normalization divides by the inflated nominal.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No entry for devicetestapp LOW-1 anywhere in MED_55_ITEM_ACCOUNTING (accounting is MED-scoped; grep finds only different-auditor 'MED-LOW-10' and 'LOW-2-class' refs). git log --all LOW hits (a20ebe685, 16b48bdb3) are the unrelated 'chapter 一千零二十四' auditor's LOW-2/3/4/5, not this file's LOW set. Current code lines 2707/2781 still use totalIters (config value). Silent drop, defect live.",
   "macFixable": true,
   "subsystem": "devicetestapp"
  },
  {
   "id": "LOW-2",
   "severity": "LOW",
   "defect": "BAS_METAL_PROBE_TIMEOUT_SEC is one env name overloaded for two features with different defaults: probe-only path defaults 8s (BASEnduranceAppRunner.swift:637) vs concurrent-heartbeat path defaults 6s (:1893). An operator tuning it for probe-only silently changes heartbeat-timeout semantics.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No devicetestapp LOW-2 entry in the accounting (the 'LOW-2' strings in the JSON at lines 134/286/293 are hostkit/mlx-decode/M-f auditors' different findings). No git commit touches this dual-default. Current code: line 637 default '8', line 1893 default '6' — both intact. Silent drop, defect live.",
   "macFixable": true,
   "subsystem": "devicetestapp"
  },
  {
   "id": "LOW-3",
   "severity": "LOW",
   "defect": "ProbeFileLog silently degrades: FileHandle build failure sets handle=nil with NO warning line (BASProbeCommon.swift:48,53) and every write is 'try? handle?.write' swallowing disk-full/handle-death (:63); and it logs via logger.info (:57) which is memory-only by default, so post-run `log show` can't retrieve it — contradicting the runner's own lesson to use .notice for capturability.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No devicetestapp LOW-3 in the accounting. cebea725a added idleGuardedSleep to BASProbeCommon.swift (for MED-2) but did NOT touch the ProbeFileLog degradation/log-level. Current code: handle=nil silent at :48, logger.info at :57, try? handle?.write at :63 — all intact. Silent drop, defect live.",
   "macFixable": true,
   "subsystem": "devicetestapp"
  },
  {
   "id": "LOW-4",
   "severity": "LOW",
   "defect": "BAS_QUANT_KVBITS accepts any Int and passes it straight into MLX kvCacheBits (BASQuantABProbe.swift:94) with no 4|8 whitelist, while the same physical KV-bits knob on the endurance side (BAS_KV_BITS) IS whitelisted to 4|8. Passing kvb=5 wastes a device run on a mid-run MLX error. (Auditor explicitly carved this out as the one exception to the 'BASQuantABProbe CLEAN' verdict.)",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No devicetestapp LOW-4 entry in the accounting. No git commit adds a kvBits whitelist to BASQuantABProbe. Current code line 94 `if let kvb = Int(env[\"BAS_QUANT_KVBITS\"] ?? \"\")` passes any Int through with no bounds check. Silent drop, defect live.",
   "macFixable": true,
   "subsystem": "devicetestapp"
  },
  {
   "id": "LOW-5",
   "severity": "LOW",
   "defect": "Two sub-defects in BASDeviceTestApp.swift onAppear: (a) the comment 'Serial, never concurrent with MLX' (:164-165) is false — the GPU probe chain Tasks (:156,166) run in the same onAppear as endurance.autostartIfEnabled() (:188) which loads MLX, so cold-start GPU readings are contended; (b) the BAS_V12_PROBE branch (:157-162) loads a 4B model but only skips the MPSGraph chain — it does NOT guard autostartIfEnabled (:188), so BAS_V12_PROBE=1 + BAS_ENDURANCE_AUTOSTART=1 loads two 4B models → guaranteed jetsam on an 8GB device; a one-line mutex would prevent it.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No devicetestapp LOW-5 in the accounting. No git commit adds a V12/autostart mutex. Current code: comment 'Serial,never / concurrent with MLX' still at :164-165, V12 branch at :157-162 with no autostart guard, unconditional autostartIfEnabled() at :188. Silent drop, defect live.",
   "macFixable": true,
   "subsystem": "devicetestapp"
  },
  {
   "id": "LOW-6",
   "severity": "LOW",
   "defect": "Cooldown spin uses wall-clock Date() rather than the monotonic clock the rest of the runner is disciplined to use (BASEnduranceAppRunner.swift:2581 `Date().addingTimeInterval(cooldown)`; mirrored in BASQwen35MTPProbe.swift:173-175). NTP step-back under cellular can arbitrarily stretch/shorten the cooldown spin, violating the file's own ch1025.10 monoNowNs discipline.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No devicetestapp LOW-6 in the accounting. No git commit converts the cooldown spin to monotonic. Current code: BASEnduranceAppRunner.swift:2581 still uses Date().addingTimeInterval for the BAS_COOLDOWN_SPIN loop. Silent drop, defect live.",
   "macFixable": true,
   "subsystem": "devicetestapp"
  },
  {
   "id": "MED-2 (secondary sleep sites)",
   "severity": "LOW",
   "defect": "The MED-2 headline (two naked 30s Task.sleep in runSamplingVerify) was fixed, but MED-2's own buried caveat listing the same-mechanism secondary naked-sleep sites was never remediated: BASQuantABProbe.swift:82 (2s), BASProbeCommon.swift:216 (5s), BASSpecSpeedupProbe.swift:88 (6s), BASSpecDefaultOnProbe.swift:83/94/138/193/213 all still call bare Task.sleep, so any of them freezes the same way if the phone locks during an unattended run.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Accounting closes MED-2 via cebea725a, whose diff routes ONLY the two BASQwen35MTPProbe 30s sleeps to idleGuardedSleep. grep -rln idleGuardedSleep returns only BASProbeCommon.swift (defn) + BASQwen35MTPProbe.swift — the secondary sites are untouched and still bare Task.sleep in current code. This secondary-site cluster is a distinct buried sub-finding with explicit file:lines that entered no ledger row; the fix helper already exists (Mac-compilable) so it is mac-fixable.",
   "macFixable": true,
   "subsystem": "devicetestapp"
  },
  {
   "id": "hostkit-rest LOW-1",
   "severity": "LOW",
   "defect": "BASAgentFabricMultiRoundLoop wireRosterToGraph failure is caught and returned as .inert whose stopReason 'mode-inert' means mode-not-authoritative, masking a real roster-assembly failure and misdirecting triage",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No hostkit-rest LOW entry exists in the MED-only 94-item ledger. git log 'MultiRoundLoop'/'mode-inert' → no remediation commit. The '全面 audit LOW-1..5' commits (a20ebe685/16b48bdb3) are chapter-1024 audit findings, not hostkit-rest.",
   "macFixable": true,
   "subsystem": "hostkit-rest"
  },
  {
   "id": "hostkit-rest LOW-2",
   "severity": "LOW",
   "defect": "BASUpdateTicketLifecycleForbiddenGate.submitWithForbiddenGate returns .proposed after absorbing duplicateTicket even when the existing entry is .rejected/.trialing → distorted return value and inflated batch accepted count (same class as MED-2)",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Absent from the accounting ledger; no git commit references BASUpdateTicketLifecycleForbiddenGate count/ingest semantics. Only ZoneGate (not the plain ForbiddenGate) was touched by 42ec9cab3.",
   "macFixable": true,
   "subsystem": "hostkit-rest"
  },
  {
   "id": "hostkit-rest LOW-3",
   "severity": "LOW",
   "defect": "pipeline canonicalRoles accepts aliases (sentinel/sovereign/evolution/hostalign) but BASAgentTierActivationValidator's known-name set only has enum rawValues → a valid alias yields a self-contradictory tier.mismatch...agent=<alias>(unknown) audit signal",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the ledger; git log for canonicalRoles / validator vocab shows no remediation. BASAgentFabricHostPipeline.swift untouched post-audit.",
   "macFixable": true,
   "subsystem": "hostkit-rest"
  },
  {
   "id": "hostkit-rest LOW-4",
   "severity": "LOW",
   "defect": "BASEBrainHostRuntimeModeAdvisoryLedger advisories array grows unbounded (manual reset only) and BASKVCacheRegistry default init (.explicitOnly + capacity nil) never invalidates → unbounded KV-byte accumulation (jetsam axis) at default settings",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the ledger. git 'KVCacheRegistry'/'ModeAdvisoryLedger' hits (1ad51a9fa, d556c89e4, 287f7d0e6, etc.) are all pre-audit M-numbered feature commits that BUILT the LRU/TTL opt-in the finding already acknowledged; none change the risky DEFAULT the finding flags.",
   "macFixable": true,
   "subsystem": "hostkit-rest"
  },
  {
   "id": "hostkit-rest LOW-5",
   "severity": "LOW",
   "defect": "BASTrainingDataExporter destination swap is non-atomic (removeItem old → moveItem new) despite an 'Atomic rename' comment; on moveItem failure the catch also deletes tempURL → both old and new export vanish; fix is replaceItemAt",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the ledger; git 'TrainingDataExporter'/'replaceItemAt'/'atomic rename' → no remediation commit. The 16b48bdb3 LOW-5 commit is a chapter-868 file-header revision, unrelated.",
   "macFixable": true,
   "subsystem": "hostkit-rest"
  },
  {
   "id": "hostkit-spine F6",
   "severity": "LOW",
   "defect": "layerTimingsMs is included in BASEBrainTurnResult's synthesized Equatable but excluded from CodingKeys — any == parity check between two independent runs of the same request false-fails on wallclock ms, and a Codable round-trip silently drops the field making == judge nil != non-nil.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "The MED_55_ITEM accounting tracks only hostkit-spine F1..F5 (verified via JSON scan; F6-F9 absent). No commit references layerTiming/this asymmetry. Current code EBrainTurnResult.swift:15 still declares `public var layerTimingsMs: [String:Double]?` and CodingKeys (1162-1215) still omit it — defect intact.",
   "macFixable": true,
   "subsystem": "hostkit-spine"
  },
  {
   "id": "hostkit-spine F7",
   "severity": "LOW",
   "defect": "ssmCautionOperatorEnabled flag is threaded through the host synthesis face but the constructed BASEBrainTurnRequest never populates turnHistory/priorSSMState and the face exposes no ssmCautionObservationSink, so the chapter-188 'sustained-pressure trajectory temporality' is permanently unreachable — operator enabling the flag gets a permanently stateless operator, a silent gap vs documented semantics.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Absent from accounting (only F1-F5 tracked). No fix commit. EBrainHostRuntimeSynthesis.swift still threads ssmCautionOperatorEnabled (lines 60/74/141/149/204/334/368/492) while the request builders at 157-165/495-505 supply no turnHistory/priorSSMState and no observation sink — defect intact.",
   "macFixable": true,
   "subsystem": "hostkit-spine"
  },
  {
   "id": "hostkit-spine F8",
   "severity": "LOW",
   "defect": "ProvisionalVerdict's 'proxy never UNDER-states the need for a protected write lane' claim is false: production normalizeUpdateTickets forces requiresReview when riskLevel>=.high (reachable with mode==.answer), raising the final verdict to .memoryFreeze, while the pre-render proxy (mode in {delay,replace,block} or requireReviewedWrites kill-switch) predicts a lower level. Observation-only, but the Phase-C fidelity claim is a live comment-lie.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Absent from accounting (only F1-F5 tracked). No fix commit. EBrainRuntimeCoordinator+ProvisionalVerdict.swift:117-123 still carries the 'never UNDER-states' doc claim and 139-143 the proxy still omits any riskLevel>=.high term — defect intact.",
   "macFixable": true,
   "subsystem": "hostkit-spine"
  },
  {
   "id": "hostkit-spine F9",
   "severity": "LOW",
   "defect": "buildSovereignExecutionReceipts mints status=.executed plus synthesized latency (index+1)*12ms for actuation commands that were never dispatched (substrate has no real executor) — 'executed' in the audit stream is an over-statement against the honesty doctrine; the same file already precedent-fixed a comparable over-claim (ch1044 keyless-SHA rename at 1626-1631).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Absent from accounting (only F1-F5 tracked). No fix commit. EBrainRuntimeCoordinator+SovereignCommit.swift:1742-1757 still builds every receipt with `status: .executed` and synthesized latencyMs=(index+1)*12 — defect intact.",
   "macFixable": true,
   "subsystem": "hostkit-spine"
  },
  {
   "id": "memory-a F6",
   "severity": "LOW",
   "defect": "upsertNotesRow (attachNotes path) comments 'Wrap in a transaction for atomicity' but the main-table UPSERT, FTS5 DELETE, and FTS5 INSERT run as three separate autocommit statements with no BEGIN/COMMIT — a process death between steps desyncs memory_usage_record_notes from its FTS5 index (stale or missing FTS rows).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "The 94-item accounting has entries for memory-a F3/F4/F5 only; F6 has no med_id row and is absent from the unaccounted_items list too. Current code Sources/BASMemory/BASMemoryUsageTracker+ReplayAuditFTS.swift: upsertNotesRow spans lines 392-473 and contains the lying comment at line 424 but NO BEGIN/COMMIT; the first BEGIN IMMEDIATE in the file is at line 536 (a different function, purgeTombstonedRows). Commit fbc22d5f7 (H11/H12) modified purge/GC to delete notes+FTS but did not wrap the notes upsert in a transaction. git log --all has no F6/notes-atomicity remediation for this file. The defect is intact.",
   "macFixable": true,
   "subsystem": "memory-a"
  },
  {
   "id": "memory-a F7",
   "severity": "LOW",
   "defect": "BASSQLiteMemoryAtomStore.admit() uses `try?` to swallow fetch errors, so a decode-corrupt existing row makes existed==false → admit returns true ('new row') and upsertAtom silently overwrites the corrupt row — return value is falsified and corruption is masked, contradicting the file's own 'integrity outranks availability' (M91) stance.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No med_id entry for memory-a F7 in the 94-item accounting, and not in unaccounted_items. Current code Sources/BASMemory/BASSQLiteMemoryAtomStore.swift:400-401 still reads `let existed = (try? Self.fetchAtom(db: db, atomID: atom.id.uuidString)) != nil` followed by `return !existed`. The `try?` swallow is unchanged. git log --all F7 hits are memory-b F7 (d91c2374a) and x-test-integrity F7 (8962e922b) — different findings in different files; none touch admit(). The defect is intact.",
   "macFixable": true,
   "subsystem": "memory-a"
  },
  {
   "id": "memory-b F10",
   "severity": "LOW",
   "defect": "BASMemoryAtomReducer non-UUID admitted-event fallback `UUID(uuidString: payload.atomID) ?? UUID()` mints a fresh random id every replay for a corrupt/cross-version/forged event → violates replay byte-determinism (silent drift).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Grep of the 94-item accounting JSON for reducer/UUID/F10 returns 0 hits — no ledger row. git log for Sources/BASMemory/BASMemoryAtomReducer.swift shows only pre-audit commits (M942 0f08dce3a, M955 13a48f8e1). Current code line 149 still `UUID(uuidString: payload.atomID) ?? UUID()`. Silently dropped, defect present.",
   "macFixable": true,
   "subsystem": "memory-b"
  },
  {
   "id": "memory-b F11",
   "severity": "LOW",
   "defect": "Event-sourced remove()/updateGovernanceStatus discard the appendEventAndUpdateCache Bool (`_ = try await ...`); if wasNew=false (eventID collision / replay-dedup hit) the removal event is not actually appended yet the function still returns the removed atom and clears contentCache → atom remains in projection while store reports deleted.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry (grep=0 for F11). The F4 fix (commit 2c1125467) only added an onSilentFailure hook to the catch block; it did NOT check the discarded wasNew result. Current code BASEventSourcedMemoryAtomStore.swift remove() still `_ = try await appendEventAndUpdateCache(payload: payload)` then clears contentCache and returns removed unconditionally on the non-throwing path. Defect present (low probability under default UUID factory).",
   "macFixable": true,
   "subsystem": "memory-b"
  },
  {
   "id": "memory-b F13",
   "severity": "LOW",
   "defect": "BASSleepConsolidationDriver doc for the SECONDARY (OS-fired) path references `AppleBGTaskSchedulerBridge.registerConsolidationLaunchHandler`, a symbol that does not exist (only a generic registerLaunchHandler exists) → a host wiring the OS background window per the run-book finds no such integration point.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry (grep=0). grep -rn 'registerConsolidationLaunchHandler' Sources/ returns only the doc comment itself at BASSleepConsolidationDriver.swift:49 — the referenced API symbol has no definition anywhere in Sources/. Doc-level defect still present, never ledgered or committed.",
   "macFixable": true,
   "subsystem": "memory-b"
  },
  {
   "id": "metal #5 (LOW — dormant layer_norm .metal dispatch recipe)",
   "severity": "LOW",
   "defect": "BASLayerNormKernel.metal header 'official dispatch recipe' is wrong: gridSize=MTLSize(width:1,height:N) with scalar uint row [[threadgroup_position_in_grid]] reads the x-component (always 0), so any future caller wiring these hand-written LayerNorm/Softmax/Reduce .metal symbols per the comment gets every threadgroup computing row 0 (rest uninitialized/zero). Same-batch kernels also lack a row bounds guard.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No entry in the 94-item MED accounting (only metal #3 and metal #4 present; this is a LOW, silently dropped). git log --all shows no fix commit for the dispatch recipe / row-recipe; the only commit touching BASLayerNormKernel.metal (5d3470b13) predates the audit. Current header comment at lines 22-23 is unchanged ('gridSize = MTLSize(width: 1, height: N, depth: 1)') and all layer_norm_* kernels (:38,:73,:105,:145,:186) still declare scalar 'uint row [[threadgroup_position_in_grid]]'. Symbols remain dormant: no Swift Metal dispatcher references layer_norm_forward/affine/backward (the only Swift hit, bas_ranker_layer_norm_affine_simd, is a separate Rust/C SIMD path). Defect present, low harm (dormant), but not verifiably closed.",
   "macFixable": true,
   "subsystem": "metal"
  },
  {
   "id": "LOW-12",
   "severity": "LOW",
   "defect": "streamDraft/streaming path completely bypasses the pressure ladder AND P0 experience load/persist — long streaming-UI sessions get zero rung sampling and never load/write the acceptance-profile experience file, even with BAS_PRESSURE_LADDER=1.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No entry for LOW-12 anywhere in the 94-item accounting (it tracks only mlx-adapter-core MED-5..11); no closure commit in git log --all for any streaming/pressure/experience wiring. Verified live: `grep -c _pressureCheck|_ensureExperienceLoaded|_persistExperienceIfDue Sources/BASMLXAdapter/MLXOrganAdapter+Streaming.swift` returns 0. Silent drop.",
   "macFixable": true,
   "subsystem": "mlx-adapter-core"
  },
  {
   "id": "LOW-13",
   "severity": "LOW",
   "defect": ".draftModelSpec arm has no fail-close (unlike every other fallible arm), and unloadDraftModel's 'actor serializes in-flight draft' comment is false under reentrancy; latent because 缝6 hardcodes draftModelTelemetryAvailable=false, but becomes a real crash lane if the model-spec lane is ever re-enabled.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry. The H1 fix (48eb7d8c4) added do/catch only to .promptLookup/.suffixLookup; verified current Sources/BASMLXAdapter/MLXOrganAdapter+Executor.swift:65-69 shows `.draftModelSpec` still returns `try await _draftSpeculative(request)` with NO do/catch, while sibling .mtpSpecSampling has one. Defect + lying comment still present.",
   "macFixable": true,
   "subsystem": "mlx-adapter-core"
  },
  {
   "id": "LOW-14",
   "severity": "LOW",
   "defect": "fusedTranscripts over-cap eviction uses keys.first(where:) on an unordered Dictionary (comment claims FIFO) → can evict the most-active transcript-only seat (whose whole state is the text) instead of the stalest one.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry, no commit. Verified live: Sources/BASMLXAdapter/MLXOrganAdapter.swift:1358 still `let drop = fusedTranscripts.keys.first(where: { $0 != key })` — arbitrary victim, not FIFO. Silent drop.",
   "macFixable": true,
   "subsystem": "mlx-adapter-core"
  },
  {
   "id": "LOW-15",
   "severity": "LOW",
   "defect": "BAS_MAX_LIVE_SESSIONS has no lower-bound clamp; env '0'/negative makes _evictBeyondCap evict the just-installed current seat every turn → permanent spill/restore thrash (other knobs use max(1,·)).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry, no commit. Verified live: MLXOrganAdapter.swift:347-348 `Int(env[...]) ?? 16` with no max(1,·), and :1623 `while sessionLRU.count > Self.maxLiveSessions` evicts everything when the cap is 0/negative. Silent drop.",
   "macFixable": true,
   "subsystem": "mlx-adapter-core"
  },
  {
   "id": "LOW-16",
   "severity": "LOW",
   "defect": "rung-1 'reclaim' is virtual until the disk write finishes (parked box still held by pendingSpill, KV not freed), and _persist eval/materializes lazy KV slices — adding a transient allocation at <15% headroom, so rung-1 can net-worsen memory short-term.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry for LOW-16 and no closure commit; the pressure-ladder commits a57c426e6/f52810730 address MED-6/MED-11 (latch-not-execute and cacheLimit ratchet), not the rung-1 write-timing/allocation ordering. Design-level defect left unaddressed and untracked.",
   "macFixable": false,
   "subsystem": "mlx-adapter-core"
  },
  {
   "id": "mlx-decode LOW-2",
   "severity": "LOW",
   "defect": "Under 2-slot concurrency two cold-start MTP turns both read priorBox==nil and each build a ~300MB quantized decoder inside container.perform (transient ~600MB overlap + EMA loss), risking jetsam on machines near the 135MB margin.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "This is the same finding as 'mlx-adapter-core MED-8' in the accounting (its detail explicitly notes 'MED-8=mlx-decode LOW-2'). Disposition = 'deferred' with remediation 'DEFERRED (device follow-up): ... deduping it safely requires restructuring the hottest decode path ... deferred to avoid destabilizing production decode.' No commit; the double-build in MLXOrganAdapter+MTPSpec.swift:51,95,140,199 is unchanged. Tracked-open, not closed.",
   "macFixable": false,
   "subsystem": "mlx-decode"
  },
  {
   "id": "mlx-decode LOW-3",
   "severity": "LOW",
   "defect": "BASDifficultyProbe has no input robustness validation: init(weightsURL:) accepts sd[i]==0 / NaN weights; successProbability divides by sd[i] with no zero/finite guard -> z=NaN -> pSuccess=NaN -> refinedBudget's two comparisons both false -> silently no-refine and box.report records NaN. Degenerate files should be rejected at init.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No 'mlx-decode LOW-3' entry exists in the 94-item MED-level accounting (LOW-scoped, silently outside the ledger). Current BASDifficultyProbe.swift init(weightsURL:) only guards w.count==mu.count==sd.count (line 29); it does NOT check sd[i]!=0 or isFinite, and successProbability (line 54) still computes `z += w[i]*(hidden[i]-mu[i])/sd[i]` with no zero-divisor/NaN guard. The nearest git hit, commit 870052374 'M-j tie-correct probe AUC + strict candidate JSON (no bare NaN)', is a Python tools-scripts fix (fit_difficulty_probe / candidate JSON) and does NOT touch BASDifficultyProbe.swift. Defect still live.",
   "macFixable": true,
   "subsystem": "mlx-decode"
  },
  {
   "id": "mlx-decode LOW-1",
   "severity": "LOW",
   "defect": "macOS default-ON MTP weight resolution appends a world-writable /tmp candidate (/tmp/gdn_coreai/qwen35_mtp_folded.safetensors), letting any local process pre-plant a forged safetensors that gets loaded into the default-ON decode path (performance-destruction + safetensors parse surface; emission stays trunk-argmax so losslessness holds; iOS unaffected).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No 'mlx-decode LOW-1' entry in the 94-item accounting (LOW-scoped, outside the MED ledger). git log grep for /tmp path / gdn_coreai / world-writable / weight-resolve returns nothing. Current MLXOrganAdapter.swift:173 still has `candidates.append(URL(fileURLWithPath: \"/tmp/gdn_coreai/qwen35_mtp_folded.safetensors\"))` (comment at :159 confirms it is the Mac-dev candidate). Dev-machine hygiene defect still present.",
   "macFixable": true,
   "subsystem": "mlx-decode"
  },
  {
   "id": "LOW-1",
   "severity": "LOW",
   "defect": "Zero cancellation checks across the entire BASOrchestration module; multi-store sequential writes (BASAuditPipeline.recordTurn 5-store, BASRoutedMirrorBladeRecording per-item append) leave half-written audit trails if the host cancels the turn Task, and because every PerTurnInput field is optional a torn turn is indistinguishable from a legitimately sparse turn on replay (no completion tombstone).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No LOW findings from orchestration appear in the accounting. `grep -rn checkCancellation|isCancelled|CancellationError Sources/BASOrchestration/` returns 0. The H18 cancellation commit (8a5e5d17b) touched BASChatCompletionsOrganAdapter (a different module), not the audit pipeline. Untracked and live.",
   "macFixable": true,
   "subsystem": "orchestration"
  },
  {
   "id": "LOW-2",
   "severity": "LOW",
   "defect": "BASAgentTraceLogEventLogBridge.recordEvent performs two awaits (traceLog.append then eventLog.append) inside the actor; concurrent calls can interleave so traceSeq 5/6 land in the event log as 6,5 — downstream consumers reading by event-log sequence see out-of-order agent-trace unless they parse the traceSeq embedded in eventID.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. Current BASAgentTraceLogEventLogBridge.swift:111-134 still does `let traceSeq = await traceLog.append(event)` then `await eventLog.append(...)` with an actor suspension point between them — the reentrant interleave the auditor described. No remediation commit.",
   "macFixable": true,
   "subsystem": "orchestration"
  },
  {
   "id": "LOW-3",
   "severity": "LOW",
   "defect": "Boundary inputs silently collapse to strong-semantic defaults: ProviderReleaseCore unknown traceKindRawValue → silently `.primary`; OrchestrationCore unknown kindID → silently `.capture` (triggers checkpoint + running). System boundary should fail-fast or at least leave an audit trace.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. Current ProviderReleaseCore.swift:105 still `BASAdaptiveTraceKind(identifier: traceKindRawValue) ?? .primary`; OrchestrationCore.swift kind(from:) still defaults unknown ids to .capture. No remediation commit.",
   "macFixable": true,
   "subsystem": "orchestration"
  },
  {
   "id": "LOW-4",
   "severity": "LOW",
   "defect": "EBrainNeuralMaterializationCore Swift fallback dominance sort uses `sorted { score(lhs) > score(rhs) }` with NO candidateID tiebreaker (unlike BASAgentFabricAdapters:388-394 which deliberately adds one), so on tied scores the Rust path and the non-Apple Swift-fallback path produce different dominanceOrder, breaching the ch838 fallback-determinism / byte-equality contract on ties.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. Current EBrainNeuralMaterializationCore.swift:318-323 fallback still `sorted { candidateDominanceScore(lhs) > candidateDominanceScore(rhs) }` with no .candidateID decider. The MED-2 fix (13a7e5b66) touched BASMLMemoryService, not this fallback. Live.",
   "macFixable": true,
   "subsystem": "orchestration"
  },
  {
   "id": "CLEAN-caveat: BASWorldAwareRiskBridge stale comment",
   "severity": "LOW",
   "defect": "Comment at BASWorldAwareRiskBridge.swift:189-192 claims 'AND the caller's existing assumption with L4's view' but the code just does `let evidenceSufficient = assessment.evidenceSufficient` — ProposedIntent has no such field. Comment lies about the logic (an evidence-sufficiency semantics documentation defect on the verdict-context path).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Flagged inside the evidence file's CLEAN section as a caveat. Not in the accounting. Current BASWorldAwareRiskBridge.swift:185-192 still shows the stale comment above `let evidenceSufficient = assessment.evidenceSufficient`. No remediation.",
   "macFixable": true,
   "subsystem": "orchestration"
  },
  {
   "id": "organ-eval LOW-1",
   "severity": "LOW",
   "defect": "profiler observing() has no input validation and Store isSane rejects the WHOLE snapshot: one accepted>proposed fold -> emaHitRate>1 -> next load fails isSane -> entire 7-day acceptance snapshot silently discarded (cold-start with no reason).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "The 94-item accounting tracks only organ-eval MED-1..5; LOW-1 has NO entry. No remediation commit: git log post-audit on Sources/BASOrgan/BASAcceptanceProfiler.swift shows nothing. Current BASAcceptanceProfiler.swift:56-61 observing() still computes `acceptedPerRound = Double(accepted)/Double(rounds)` with no clamp and no accepted<=proposed guard; Store isSane still whole-snapshot reject. Silent drop the section-12 sweep itself missed.",
   "macFixable": true,
   "subsystem": "organ-eval"
  },
  {
   "id": "organ-eval LOW-2",
   "severity": "LOW",
   "defect": "kill-switch path (decodePlannerAutoSelect=false) skips supportedRoles validation / pressure sampling / turn-line, so an unsupported role returns success instead of throwing unsupportedRole (base entry throws) — contract not equivalent; pressure ladder blind on that path.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting (only MED-1..5 tracked). H21 (afbbcc93e) fixed the HIGH-1 sessionID divergence in the same file but NOT this sub-defect. Current Sources/BASMLXAdapter/MLXOrganAdapter+PromptLookup.swift:174-176 still `guard decodePlannerAutoSelect else { return try await _plainDraft(request) }`, bypassing role/pressure/turn handling. No commit addresses the kill-switch contract equivalence.",
   "macFixable": true,
   "subsystem": "organ-eval"
  },
  {
   "id": "organ-eval LOW-3",
   "severity": "LOW",
   "defect": "sleep-station ledger append is best-effort (try? swallows disk-full/permission errors) on a verdict-only station whose ONLY persisted product is that ledger; and every suite record shares one startedAtMs stamped at station entry (晨读 sort/staleness distorted).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. Current Sources/BASEvaluation/BASSleepMeasurementStation.swift:281 still `try? appendToLedger(records, ledgerURL: ledgerURL)` (swallowed) and startedAtMs is stamped as entry-time nowMs at :190 and :260 for all records. Post-audit commits to the file (983ea4c4f MED-1 race, 2e2483e05 H20 zombie) address different defects; LOW-3 unremediated.",
   "macFixable": true,
   "subsystem": "organ-eval"
  },
  {
   "id": "organ-eval LOW-4",
   "severity": "LOW",
   "defect": "SaguaroLoop last-round accepted stat inflated: `accepted += acc` counts all matched tokens before the EOS check, so if EOS falls inside the accepted span the final round over-counts by up to K-1 (telemetry-only, affects meanAccepted).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. Current Sources/BASOrgan/BASSaguaroLoop.swift:106 still `accepted += acc` ahead of the EOS handling (loop `for i in 0...acc` at :109). No post-audit commit touches BASSaguaroLoop.swift. Silently dropped LOW.",
   "macFixable": true,
   "subsystem": "organ-eval"
  },
  {
   "id": "LOW-1",
   "severity": "LOW",
   "defect": "Calibration bundle/sub-model version monotonicity uses string comparison, so a legitimate v9→v10 upgrade is hard-rejected ('v10.0.0' < 'v9.0.0' lexically) — BASRiskCalibrationGate.swift:166-171 and BASRiskCalibrationStratumSubModel.swift:254-263.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No policy-obs LOW item exists anywhere in MED_55_ITEM_ACCOUNTING_2026-07-09.json (grep of the cited filenames returns nothing). git log shows zero post-audit commits on either file. Current code at BASRiskCalibrationGate.swift:167 still reads `guard proposedVersion > currentVersion` (raw String >), and the stratum model at :256 same. Defect intact.",
   "macFixable": true,
   "subsystem": "policy-obs-misc"
  },
  {
   "id": "LOW-3",
   "severity": "LOW",
   "defect": "replayFingerprint degrades to a constant fingerprint on encode failure: `(try? encoder.encode(bundle)) ?? Data()` → SHA256(empty) whenever any Double in the bundle is NaN/∞, so distinct bundles collide to one fingerprint and audit dedup/verification silently mis-compares — ObservabilityCore.swift:751-761.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. No post-2026-07-06 commit touches ObservabilityCore.swift. Read of ObservabilityCore.swift:753 confirms `let data = (try? encoder.encode(bundle)) ?? Data()` unchanged — the empty-Data fallback is still the fingerprint source.",
   "macFixable": true,
   "subsystem": "policy-obs-misc"
  },
  {
   "id": "LOW-4",
   "severity": "LOW",
   "defect": "'latest' snapshot fingerprint is actually the earliest: latestSnapshotFingerprintByKind is computed via firstValueByKind over time-appended traces, so the dashboard's 'newest brain snapshot' shows the oldest value and drift detection watches the wrong object — ObservabilityCore.swift:1341.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. No post-audit commit on ObservabilityCore.swift. Read of line 1341 confirms `latestSnapshotFingerprintByKind: firstValueByKind(from: traces.map { ($0.kind, $0.snapshotFingerprint) })` — still firstValueByKind, not a last/most-recent selector.",
   "macFixable": true,
   "subsystem": "policy-obs-misc"
  },
  {
   "id": "LOW-5",
   "severity": "LOW",
   "defect": "BASBreathScheduler.schedule() duplicate-check runs BEFORE `await bridge.register`, so two concurrent same-id schedules both pass the dup check and both register (duplicateRequest contract void under concurrency); and `_ = await bridge.register(...)` ignores an OS registration refusal yet still records the breath as scheduled — BASBreathScheduler.swift:105-129.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "The accounting DOES track a co-hit (x-concurrency MED-LOW-10, closed by cfc103ad0) but that commit only rewrote cancelAll (the while-drain at :134-139). Read of the current schedule() at :110-120 confirms `if scheduled[request.id] != nil { throw }` still precedes `await bridge.register(request)`, and `_ = await bridge.register(request)` still discards the result. LOW-5's scenario-A schedule-side TOCTOU and scenario-B ignored-register residual have no commit and no ledger row.",
   "macFixable": true,
   "subsystem": "policy-obs-misc"
  },
  {
   "id": "LOW-2",
   "severity": "LOW",
   "defect": "Two SQLite read paths treat a step error as normal end-of-results: `while sqlite3_step == SQLITE_ROW` ends the loop on SQLITE_BUSY/SQLITE_CORRUPT and returns a partial dataset as complete; and BASUpdateTicketLifecycleSQLiteStorage.performLoad silently `continue`s past rows with NULL id/json columns — BASRiskObservationsSQLiteStorage.swift:252/312 and BASUpdateTicketLifecycleSQLiteStorage.swift:136-158.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Only the corrupt-at-OPEN half was closed: commit af4b38b9f added a PRAGMA integrity_check open-gate to BASRiskObservationsSQLiteStorage.swift (acknowledged under MED-2's accounting detail). But integrity_check runs at open, not mid-step; a SQLITE_BUSY during the step loop still ends silently. Read of BASUpdateTicketLifecycleSQLiteStorage.swift:144-148 confirms the NULL-column `else { continue }` skips are unchanged. LOW-2 has no standalone accounting entry.",
   "macFixable": true,
   "subsystem": "policy-obs-misc"
  },
  {
   "id": "LOW-6",
   "severity": "LOW",
   "defect": "scheduleBreath and withLiveThermalGuardLevel prefer a possibly-stale cached thermal reading over a fresh sample: `if let cached = currentReading() { use it } else { sample() }`, so when notifications aren't observed and turns are sparse a minutes-old nominal reading gates heavy maintenance/budget while the device is already serious/critical — BASLeaseLifeCoordinator.swift:127-134 and BASBudgetFrame+LiveThermal.swift:107-114.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. No post-audit commit on either file. Reads confirm both still do `if let existing/cached = ...currentReading() { reading = existing } else { reading = ...sample() }` — cached-first ordering intact; sample() (cheap) is not preferred.",
   "macFixable": true,
   "subsystem": "policy-obs-misc"
  },
  {
   "id": "LOW-7",
   "severity": "LOW",
   "defect": "BASDeviceRouting throttle branch disables ANE (`return thermalGuard != .throttle`) and routes to GPU, with a comment 'NPU is high-throughput; under throttle we want lower-power GPU' whose power premise is inverted for Apple Silicon (project self-measurement: ANE is the low-power engine, GPU the energy-hungry one) → throttle routes to the hotter engine, accelerating escalation to emergency — BASDeviceRouting.swift:92-98.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. No post-audit commit on BASDeviceRouting.swift. Read of :92-98 confirms both the `thermalGuard != .throttle` ANE gate and the disputed comment are unchanged. (Auditor flagged as intentional-design-but-premise-questionable; still uninvestigated.)",
   "macFixable": true,
   "subsystem": "policy-obs-misc"
  },
  {
   "id": "LOW-8",
   "severity": "LOW",
   "defect": "Vault.registerBridge half-registers: the bridge is unconditionally inserted into bridgesByID but only appended to a horizon `if horizons[bridge.sourceDomain]` exists; when sourceDomain has no horizon the bridge is findable by bridge(id:) yet invisible to outboundBridges(from:), so the counterfactual seeder never uses it — BASWorldPriorVault.swift:166-178.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. No post-audit commit on BASWorldPriorVault.swift. Read of :166-178 confirms `bridgesByID[bridge.id] = bridge` runs unconditionally while the horizon append is guarded by `if var horizon = horizons[bridge.sourceDomain]` — no else branch, no validation that the sourceDomain horizon exists. Defect intact.",
   "macFixable": true,
   "subsystem": "policy-obs-misc"
  },
  {
   "id": "runtimecore-a #5 (LOW)",
   "severity": "LOW",
   "defect": "BASEffortSignals.surprise(fromMSE: +∞) returns NaN (violates documented [0,1) range); allocator clamp01(NaN)=0 inverts it to 'zero surprise', routing the most out-of-distribution turn to reflex/fast tier.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry (JSON grep for surprise/effortsignal/isFinite → no matching item). BASEffortSignals.swift history is only the two biomimetic feature commits 01ff0d2a9/8fac2e8e8 — neither a NaN fix; no post-audit touch. Current code (line 21) guards only `mse > 0, scale > 0`, which PASSES for +∞ → returns ∞/(∞+1). Verified with a Swift snippet: result = nan. The recommended `guard mse.isFinite else { return 1 }` was never applied.",
   "macFixable": true,
   "subsystem": "runtimecore-a"
  },
  {
   "id": "runtimecore-a #6 (LOW)",
   "severity": "LOW",
   "defect": "BASWallclockNanos.defaultV1Nanos() computes UInt64(Date().timeIntervalSince1970 * 1e9); a wallclock set before 1970 (NTP anomaly / manual clock change) makes the Double negative → UInt64() conversion traps and crashes the process; ordinary NTP backsteps make the 'monotonic' reading run backwards, underflowing consumer diffs.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry. No post-audit commit touches BASCSystemProbes.swift. Current BASCSystemProbes.swift:81-83 still returns `UInt64(Date().timeIntervalSince1970 * 1_000_000_000)` unguarded. Defect intact.",
   "macFixable": true,
   "subsystem": "runtimecore-a"
  },
  {
   "id": "runtimecore-a #7 (LOW)",
   "severity": "LOW",
   "defect": "Under critical thermal (headroom < 0.15), a .guarded request (critic=3/toolVerification=3, raised for isolation/recovery risk) is capped to .fast → criticStrength=0, toolVerificationStrength=0: the highest-risk turn runs with ZERO verification on the hottest device.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry. No post-audit commit touches BASEffortAllocator.swift/BASEffortPlan.swift. Current BASEffortAllocator: rank(.guarded)=1 (balanced rung) and levelForHeadroom(critical)→.fast (rank 0); capped = rank(cap) < rank(base) → applied=.fast, stripping all safety knobs. The rank() comment even documents guarded 'only yields to a critical one' — the hazard is by-design and unresolved (audit: 'should be decided before adoption'; effort not yet consumed by runTurn).",
   "macFixable": true,
   "subsystem": "runtimecore-a"
  },
  {
   "id": "runtimecore-b LOW #9 (evidence item #9)",
   "severity": "LOW",
   "defect": "BASAutoRouteRanker+Tribunal / +RiskPlane force-unwrap baseAddress on caller-supplied JSON: empty input string → Array(inputJSON.utf8) empty → withUnsafeBufferPointer baseAddress is nil → `ip.baseAddress!` traps.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Absent from the 94-item accounting (only runtimecore-b MED-2..6 are tracked). Current code unfixed: +Tribunal.swift:107/121/135 and +RiskPlane.swift:103/107 still `baseAddress!` with no `isEmpty → body(nil,0)` guard (the `if needed==0 return \"\"` check is AFTER the first unwrap). The only baseAddress! git fix (5e1a713e0) touches a DIFFERENT file (BASCxxVectorIndexBridge), not these two. Sibling call sites in BASInternalRustBridges have the guard; these do not.",
   "macFixable": true,
   "subsystem": "runtimecore-b"
  },
  {
   "id": "runtimecore-b LOW #10 (evidence item #10)",
   "severity": "LOW",
   "defect": "BASSQLiteKnowledgeGraphStorage.removeNode does deleteIncidentEdges then deleteNode as two separate statements with no transaction; process death / second-statement failure between them leaves edges deleted but node present (prune intent unmet, undetectable).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Absent from the accounting. Current code BASSQLiteKnowledgeGraphStorage.swift:262-270 still calls `try Self.deleteIncidentEdges(...)` then `return try Self.deleteNode(...)` with no BEGIN IMMEDIATE wrapper, unlike the event-log store's pruneBefore which the finding cites as the pattern to copy. No git commit addresses removeNode/deleteIncidentEdges transactionality.",
   "macFixable": true,
   "subsystem": "runtimecore-b"
  },
  {
   "id": "runtimecore-b LOW #7 (evidence item #7)",
   "severity": "LOW",
   "defect": "BASEventLogBinaryCodec.decode indexes the input Data with absolute subscripts (buf[pos] from pos=0, buf.subdata(in: pos..<pos+len)); a non-zero-startIndex Data slice (e.g. blob[1...]) causes out-of-bounds crash or reads wrong bytes.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Absent from the accounting. Current code BASEventLogBinaryCodec.swift:281 starts `var pos = 0` and uses absolute `buf[pos]` (286,297,300,311,357-360,374) and `buf.subdata(in: pos..<pos+len)` (389) with no `let buf = Data(buf)` normalization at entry and no relative-index conversion. No fix commit in git log; only the original feature commit af5bece5c exists.",
   "macFixable": true,
   "subsystem": "runtimecore-b"
  },
  {
   "id": "runtimecore-b LOW #8 (evidence item #8)",
   "severity": "LOW",
   "defect": "Process-global nonisolated(unsafe) mutable feature flags (runIntegrityCheckOnOpen/useBinaryPayload/rowIntegrityChainEnabled) have no synchronization; runtime flip concurrent with append is a Swift data race, and one append reads two flags mid-flight, permitting the explicitly-excluded 'binary row + integrity chain' combination.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Absent from the accounting. Current code BASSQLiteEventLogStorage.swift:115/122/132 still `public nonisolated(unsafe) static var ... = false` with no OSAllocatedUnfairLock wrapper and the convention remains a soft doc comment rather than a hard assertion. No remediation commit found.",
   "macFixable": true,
   "subsystem": "runtimecore-b"
  },
  {
   "id": "rust LOW (chain.rs wire format)",
   "severity": "LOW",
   "defect": "bas-substrate-core/chain.rs:91 sovereign_canonical_bytes places prior_hash in the first 32 bytes (verify_sovereign_chain depends on this), whereas Swift production basSovereignAuditCanonicalBytes 1.2.0 places priorHash as the penultimate field; a watchOS C sealer and iOS Swift ledger cannot cross-verify, and the recommended interop caveat in bas_sovereign_c_abi.h was never added.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Grepped the 94-item accounting (items + all reexam sections) for chain.rs/prior_hash/watchOS — zero entries; the only rust accounting item is the KG-codec MED. git log --all shows no commit touching this layout or adding a header caveat. Current code: chain.rs:103 still extends prior_hash32 first; bas_sovereign_c_abi.h has no interop-mismatch warning (only a NULL error note at line 196). Defect stands; LOW because no cross-end verification path exists today.",
   "macFixable": true,
   "subsystem": "rust"
  },
  {
   "id": "rust LOW/NOTE (assembler.rs dead code)",
   "severity": "LOW",
   "defect": "bas-canonical-bytes/src/assembler.rs assemble_canonical_bytes/canonical_sha256 (pre-1.2.0 delimiter-join, forgeable 0x1F join that 1.2.0 exists to replace) is not no_mangle-exported and has no non-test caller; preventive note flags risk that a new caller could misuse the old unsafe encoding.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Accounting has no entry for assembler.rs/delimiter-join. git log --all shows no removal/annotation commit. Current code: assembler.rs:43/67 still define both fns; all references (lines 99-122) are #[cfg(test)] only. Preventive, not a live defect, but silently dropped from the §12 sweep and unaddressed.",
   "macFixable": true,
   "subsystem": "rust"
  },
  {
   "id": "rust NOTE (verdict_decisions.rs:455)",
   "severity": "LOW",
   "defect": "verdict_decisions.rs:455 `let _ = c_char::from(0);` exists solely to silence an unused-import warning; harmless but confusing, removable by dropping the use std::os::raw::c_char import.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry for c_char/verdict_decisions:455. git log --all shows no cleanup commit. Current code: line 455 still reads `let _ = c_char::from(0); // silence unused import warn` verbatim. Cosmetic, unaddressed, absent from the ledger.",
   "macFixable": true,
   "subsystem": "rust"
  },
  {
   "id": "LOW-8",
   "severity": "LOW",
   "defect": "verifyChainIntegrity/auditChainFull recompute selfHash via CryptoKit hash() while append seals selfHash via the Rust routed seal (hashViaAutoRouter) when useRoutedSeal=true — a dual hash-implementation coupling; a single 1-byte divergence between Rust sha2 and CryptoKit would selfHashMismatch EVERY entry and false-quarantine the whole ledger on reload.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Evidence file lines 33-35 (BASSovereignAuditLedger.swift :526-530 seal vs :632/:787 verify). The 94-item accounting (MED_55_ITEM_ACCOUNTING_2026-07-09.json) contains ONLY sovereign MED-3/4/5/6 — no entry for LOW-8. git log --all grep for LOW-8/dual-hash returns only ch-1044-audit commits (56a6138ba 'LOW-8 deps') which are a DIFFERENT audit, not this megaaudit finding. Current code confirms the split is unchanged: auditChainFull() line 826 uses `hash(canonical)` (CryptoKit) while append() line 544-545 uses `hashViaAutoRouter(canonical)` when useRoutedSeal. Latent (byte-equality pinned by 50+ tests, auditor labels it '非活漏洞'), but the finding is neither tracked nor remediated.",
   "macFixable": true,
   "subsystem": "sovereign"
  },
  {
   "id": "x-concurrency MED-LOW-10",
   "severity": "LOW",
   "defect": "BASMetalSyncBridge DispatchSemaphore.wait blocks the calling cooperative/actor-executor thread up to timeoutMs → pool-saturation degradation.",
   "status": "ACCOUNTED_OPEN",
   "evidence": "disposition=device-only; accounting: 'No post-audit commit to the file ... async restructure risks worse (priority inversion) and needs on-device thermal/load to certify. Left as a documented low-severity item.'",
   "macFixable": false,
   "subsystem": "synthesis"
  },
  {
   "id": "tests-arch ① LOW (env gate-variable sprawl, no manifest/registry)",
   "severity": "LOW",
   "defect": "40+ BAS_*/TEST_RUNNER_*/QINAO_* gate variables scattered across 61 files with no manifest/registry; a renamed or mistyped variable (e.g. BAS_TR_PROBE vs BAS_TR_PROBE_TEST) silently disables a probe forever with no alarm.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No entry for this LOW in the 94-item MED accounting (accounting is MED-scoped; only the 4 tests-arch MED items appear). git log shows no env-var manifest/registry commit for test gate variables — 51bb52400 is a known-flakes registry + ADR index, unrelated to the test env-gate variable set. No fix exists; silent drop.",
   "macFixable": true,
   "subsystem": "tests-arch"
  },
  {
   "id": "tests-arch ③ LOW (@MainActor/turn-driving swift-testing — no enforcement mechanism)",
   "severity": "LOW",
   "defect": "The 'turn-driving swift-testing suites must be @MainActor to avoid the 512KB-pool SIGBUS' discipline lives only in session memory; there is no lint / test base-class / CI check preventing a future non-@MainActor turn-driving @Test suite from re-introducing the SIGBUS (which, run via `swift test | tail`, would be swallowed under a green XCTest summary).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No entry in the 94-item accounting. git log SIGBUS commits (8559c37ec, 27e0fcb2e, 4a3b20830, 15829f465) are all historical point-fixes/root-causing that ADD @MainActor to specific suites; none introduce a lint or enforcement mechanism to prevent recurrence, which is precisely what this LOW asks for. No fix exists.",
   "macFixable": true,
   "subsystem": "tests-arch"
  },
  {
   "id": "tests-arch ⑤ LOW (hardcoded machine paths + EmbeddingFactBank silent skip)",
   "severity": "LOW",
   "defect": "10 source-tree-audit tests fall back to a hardcoded `/Users/changgeng/Project/…` path (BASChapter1005ScaffoldInventoryPinTests.swift:36 et al.), and BASEmbeddingFactBankCalibrationTests.swift:87 depends on `~/qwen_honesty_finetune/wikidata_facts.json`, silently skipping (and thus silently rotting) when absent.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No entry in the 94-item accounting; the two 'hardcoded' hits in the JSON are for other subsystems (H23 release-gate PASS theatre), not these test paths. git log shows no commit touching BAS_PROJECT_ROOT fallback removal or the FactBank silent-skip. No fix exists.",
   "macFixable": true,
   "subsystem": "tests-arch"
  },
  {
   "id": "tests-arch ⑤ LOW (19 default-running wall-clock threshold assertions)",
   "severity": "LOW",
   "defect": "19 wall-clock threshold assertions across 7 files run by default (e.g. BASCognitiveBrainPerformanceTests.swift:55, a 50ms ceiling), giving false-red flake risk under CI load.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No entry in the 94-item accounting. git log wall-clock/flake commits (986ba4bbc endurance time-cap, 1a6faa8bb endurance monotonic clock, 300770f08 L8 concurrent-read de-flake, memory turn_ref tiebreakers) address unrelated endurance-timing and ordering flakes, not the 19 default-running perf-threshold assertions cited here. No fix exists; silent drop.",
   "macFixable": true,
   "subsystem": "tests-arch"
  },
  {
   "id": "tools-scripts LOW (assert-based sha pre-registration pins)",
   "severity": "LOW",
   "defect": "All sha pre-registration pins in b2_r2_pipeline.py (and b2_refit_candidate.py) use bare `assert sha == pinned`; under python -O / PYTHONOPTIMIZE the entire registration/freeze gate silently vanishes (asserts stripped).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No entry for this LOW anywhere in the 94-item accounting (only the 5 tools-scripts MEDs are listed). Current Tools/b2_r2_pipeline.py still uses `assert sha == ...` at lines 76,230,238,277,280,281,313,321,478,486 (verified). File last touched 0d2d6e98a 2026-07-07 which did NOT convert asserts to sys.exit. Defect stands, silently dropped from ledger.",
   "macFixable": true,
   "subsystem": "tools-scripts"
  },
  {
   "id": "tools-scripts LOW (eval_probe_ood.py relative path + hardcoded 0.85)",
   "severity": "LOW",
   "defect": "eval_probe_ood.py:19 WEIGHTS is a cwd-relative path 'Docs/probe_weights_v1_...json' (inconsistent with sibling scripts' absolute-path convention); line ~72 hardcodes the docstring '≥ in-domain equivalent' criterion as the literal 0.85.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry for this LOW. Current code: line 19 `WEIGHTS = \"Docs/probe_weights_v1_2026-07-04.json\"` (relative) and line 72 `hi_succ >= 0.85` both still present (verified). File last touched d78a87fee 2026-07-06 (pre-audit frontier round) which did not address either. Defect stands.",
   "macFixable": true,
   "subsystem": "tools-scripts"
  },
  {
   "id": "tools-scripts LOW (check_sdk_import_boundaries.sh regex bypass)",
   "severity": "LOW",
   "defect": "check_sdk_import_boundaries.sh boundary regex only matches `^import <module>\\b`; forms like `import struct BASHostKit.X` and `@_exported import` bypass the SDK-boundary gate.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry. Current script builds forbidden_pattern='^import ${c}\\\\b' (verified lines 63-70) — still only anchors plain `import module`. File last touched 29a503d1c 2026-05-21 (pre mega-audit). Defect stands, never entered the ledger.",
   "macFixable": true,
   "subsystem": "tools-scripts"
  },
  {
   "id": "tools-scripts LOW (pre-commit-gates.sh fixed /tmp/gate.out race)",
   "severity": "LOW",
   "defect": "pre-commit-gates.sh:37 writes gate output to a predictable shared path /tmp/gate.out; two concurrent runs clobber each other's output producing spurious gate pass/fail reports (should use mktemp).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry. Current script still `> /tmp/gate.out` at line 37, `grep ^RESULT /tmp/gate.out` line 39, `rm -f /tmp/gate.out` line 48 — no mktemp (verified). File last touched 96f9510bc 2026-05-21 (pre-audit). Defect stands.",
   "macFixable": true,
   "subsystem": "tools-scripts"
  },
  {
   "id": "tools-scripts LOW (qinao_humaneval.py unsandboxed exec + except-pass)",
   "severity": "LOW",
   "defect": "qinao_humaneval.py runs model-generated code via subprocess.run([PYBIN, path]) with full user privileges and no sandbox; additionally `except Exception: pass` counts infrastructure errors as model-wrong answers.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No accounting entry for this LOW. Current Tools/qinao_local_eval/qinao_humaneval.py lines 34-45 still do subprocess.run([PYBIN, path], timeout=15) on the concatenated model program with no sandbox, and `except Exception: pass` (verified). No post-audit commit touches the file. Defect stands, silently dropped (security-relevant: arbitrary code execution).",
   "macFixable": true,
   "subsystem": "tools-scripts"
  },
  {
   "id": "x-architecture LOW-6",
   "severity": "LOW",
   "defect": "Three file-level dead islands (BASSovereignAuditCoverage.swift 153 LOC, BASMemoryTieringObservationCoverage.swift 126 LOC, BASCognitiveBrainSummaryLatencyBudget.swift 80 LOC) with zero live references, plus BASTieredCompressionProtocol.swift referenced only by deactivated tests; 179 public top-level types (6.2%) zero-referenced outside their declaring file.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No LOW entry exists in the 94-item MED_55_ITEM_ACCOUNTING (only x-architecture MED-1/2/3 are tracked). No remediation commit found in git log --all for any of the four filenames. All four files confirmed still present on disk at decode-planner HEAD. Disposition per evidence file is operator-gated git-mv-to-Archive under the deletion-aversion rule, so leaving them is defensible policy, but the finding is neither closed nor in the ledger.",
   "macFixable": true,
   "subsystem": "x-architecture"
  },
  {
   "id": "x-architecture LOW-8",
   "severity": "LOW",
   "defect": "check_god_files.sh has three stale pins (BASCognitiveBrain.swift|4500, BASAutoRouteRanker.swift|4000, BASMemoryUsageTracker.swift|3000) far above the files' current sizes, so those files can silently regrow 7-10x past the 1500 warn / 3000 error thresholds with no signal.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. No commit tightening these pins (git log hits for 'god_file' are ecdbdfb94 which ADDED the pin mechanism, and unrelated ch-1024 LOW-8). scripts/check_god_files.sh still contains the exact stale pins; current wc -l = 661 / 502 / 297 respectively, all vastly below their pins. Defect intact.",
   "macFixable": true,
   "subsystem": "x-architecture"
  },
  {
   "id": "x-architecture LOW-7",
   "severity": "LOW",
   "defect": "check_sdk_import_boundaries.sh blind spots: matches only ^import X (misses @_exported import), has no 'module-list == ls Sources/' completeness assertion, and scans only Sources/. The MED-2 fix aggravated this by adding a new 24th module BASAdminUI that is absent from the script's SUBSTRATE_CORE/SDK_CONSUMER lists, so it is now silently unchecked by the boundary gate.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. No commit hardening the script. BASAdminUI dir exists (created by MED-2 fix f7b93ccb2) but grep of scripts/check_sdk_import_boundaries.sh returns NOT LISTED; Sources now has 24 module dirs vs the 15-entry SUBSTRATE_CORE hardcoded list. The predicted 'new module lands unchecked' scenario is now live.",
   "macFixable": true,
   "subsystem": "x-architecture"
  },
  {
   "id": "x-architecture LOW-5",
   "severity": "LOW",
   "defect": "Package.swift declared-dependency vs actual-import drift: BASOrchestration declares BASLeaseLife but has zero 'import BASLeaseLife'; BASBrainCLI declares BASMemory/BASPolicy that its sources never import; BASMLXAdapter declares the Hub product with no 'import Hub' and no remaining #hubDownloader/#huggingFaceTokenizerLoader macro call sites. Manifest topology lies to auditors.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting. No remediation commit. Confirmed at HEAD: Package.swift:406 lists BASLeaseLife for BASOrchestration while grep 'import BASLeaseLife' in Sources/BASOrchestration returns nothing; Hub product still declared at Package.swift:384 with no import Hub in Sources/BASMLXAdapter. Defect intact.",
   "macFixable": true,
   "subsystem": "x-architecture"
  },
  {
   "id": "x-architecture LOW-4",
   "severity": "LOW",
   "defect": "Transitive-import fragility: 11 BASHostKit files import BASRustMemoryTrackerBinary and 25 BASMLXAdapter files import MLX/MLXNN/MLXOptimizers, but neither the BASHostKit binaryTarget dep nor mlx-swift is declared for those targets in Package.swift — visibility rides only on transitive resolution, so a vendor/intermediate-package trim breaks the link (same class as MED-3 but same-repo, hence LOW).",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Not in the accounting (only MED-3, the swift-crypto case, was declared via dfb37327b). No commit adds explicit BASRustMemoryTrackerBinary/mlx-swift declarations for these importing targets. git log for 'RustMemoryTracker'/'mlx-swift declar' returns nothing relevant. Defect intact.",
   "macFixable": true,
   "subsystem": "x-architecture"
  },
  {
   "id": "x-concurrency LOW-12",
   "severity": "LOW",
   "defect": "Lazy pipeline trio (BASMetalFlashAttentionDispatcher.ensureForward/Masked/CausalPipeline, lines 341-402) and BASEmbeddingFactBank.load do nil-check -> await -> assign, so concurrent first callers double-compile the PSO / re-embed the whole fact bank (benign waste, results equivalent). One-line 'set placeholder before await' fix.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No entry in the 94-item accounting for LOW-12 (grep of med_id/detail finds nothing). git log --all for BASMetalFlashAttentionDispatcher.swift and BASEmbeddingFactBank.swift shows NO commit since the 2026-07-06 audit; the nearest metal commit c8611b94a addresses metal#4 (cache-key epsilon), a different defect. Current source confirms the defect live: dispatcher lines 344/365/386 are `if xPipeline != nil { return }` then `try await device.makeComputePipelineState(...)` with no in-flight guard; BASEmbeddingFactBank.swift:46 `guard !loaded else { return }` then loops `await provider.embed(...)` and sets `loaded = true` only at line 53 after all awaits. Silently dropped from the x-concurrency rollup.",
   "macFixable": true,
   "subsystem": "x-concurrency"
  },
  {
   "id": "x-concurrency §三① ACCEPT-WITH-CAVEAT (CoreAI/Saguaro)",
   "severity": "LOW",
   "defect": "@unchecked Sendable exception class: the 8 BASCoreAI*Session types + BASSaguaroSpeculator carry mutable state guarded only by a convention (\"only touch on owning executor\") with NO assertion/tripwire, unlike the BASRoutedVectorIndexStorage DEBUG tripwire the auditor holds as the template. Auditor recommends adding a DEBUG in-flight assertion before multi-seat production use.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "Surfaced as a caveat inside the §三① 'CLEAN' census (judged ACCEPT-WITH-CAVEAT for current probe/single-owner usage). Not enumerated as a numbered finding, has no entry in the 94-item accounting, and no commit adds the recommended DEBUG in-flight assertion to any BASCoreAI*Session or BASSaguaroSpeculator. (Sibling BASPQIndex from the same caveat sentence WAS separately tracked as runtimecore-b MED-6 and closed by de18c7876; the CoreAI/Saguaro members were not.) Accepted-as-is rather than verifiably closed.",
   "macFixable": true,
   "subsystem": "x-concurrency"
  },
  {
   "id": "x-sovereignty #7 (LOW)",
   "severity": "LOW",
   "defect": "SampleHost stress-run report persists device identifierForVendor into a local run JSON (DeviceInfo), a device identifier landing in a copyable report file.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No entry in the 94-item MED ledger. Defect still present in current code at /Users/changgeng/Project/Project06/Project06/SampleHost/SampleHostChengluStressRunner.swift:1097 (`let identifierForVendor: String?`) and :1107 (`device.identifierForVendor?.uuidString`). No closure commit: `git log --all | grep -iE identifierForVendor|ChengluStress` shows only unrelated 01ac9e7be. The file sits OUTSIDE the BAS git repo (git ls-files does not track it), so the §12 sweep of BAS commits could not have covered it. The evidence file itself classifies this as a non-violation/informational census note ('无离机路径,非违宪;列出以备操作员知情'), so no remediation was ever pursued — it remains as-is.",
   "macFixable": true,
   "subsystem": "x-sovereignty"
  },
  {
   "id": "F8 (LOW)",
   "severity": "LOW",
   "defect": "#95 determinism_temp0 comment claims re-ask must be 'byte-identical' but the code only asserts the MCQ letter matches (letter(ask(q))==n), so different completions with the same letter still count 100 — the CRITICAL '=100' gate is weaker than its comment claims.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No 'x-test-integrity F8' entry exists anywhere in the 94-item accounting (grep of the JSON returns only F4/F5/F6/F7 for x-test-integrity; the F8 hits are memory-b F8). The MED_55 ledger silently omits the three LOW findings. Current code confirms defect stands: Tools/qinao_local_eval/qinao_eval.py:65 comment still reads '# #95 determinism: re-ask neutral, must be byte-identical' while line 66 computes det_ok += (letter(ask(q)) == n) — letter-only comparison. No commit in git log --all touches this.",
   "macFixable": true,
   "subsystem": "x-test-integrity"
  },
  {
   "id": "F9 (LOW)",
   "severity": "LOW",
   "defect": "Zero-assertion 'doctrine-pin' tests present green-test runtime claims as XCTAssertTrue(true) pure-prose tautologies (e.g. testCommentNotDeleteDoctrineHeld asserting 'all V1 fallback paths preserved/tested/active').",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No 'x-test-integrity F9' entry in the accounting. Defect still present: BASChapter786ArcSealTests.swift:162 and BASChapter781ShadowTrialInjectTests.swift:119 both still contain XCTAssertTrue(true, ...). Post-audit commits touching Chapter786 (b62591b41 XCFramework rebuild, chapter-cleanup commits) do not remove the tautological assertions. Note: tests-arch line-515 in the accounting is a DIFFERENT auditor's XCTAssertTrue(true) sub-family (48 occurrences), explicitly left 'unremediated'; the x-test-integrity F9 sites are not individually dispositioned.",
   "macFixable": true,
   "subsystem": "x-test-integrity"
  },
  {
   "id": "F10 (LOW)",
   "severity": "LOW",
   "defect": "release_gate.py docstring drifts from the actual DEFERRED_SUBSTRATE list: docstring narrates 4 non-host-authorable CRITICAL gates (#19, #97/#98 'schema-parity scripts absent', #99) but DEFERRED_SUBSTRATE holds only 2 entries (coreai_ane_conversion_fidelity, authoritative_test_suite_pass) — and #97 schema-parity actually landed. The deferred ledger, the system's honesty device, is itself stale.",
   "status": "NOT_IN_ACCOUNTING",
   "evidence": "No 'x-test-integrity F10' entry in the accounting. Current code: release_gate.py:13-14 docstring still says '#19 coreai device, #97/#98 schema-parity scripts absent, #99', while DEFERRED_SUBSTRATE at line 42-45 contains only 2 keys (neither #97/#98/#99). Schema-governance parity gates exist in the tree (BASEBrainSchemaGovernanceRegistryTests.swift), so the '#97/#98 scripts absent' claim is stale. No commit reconciles the docstring with the list.",
   "macFixable": true,
   "subsystem": "x-test-integrity"
  }
 ],
 "tranche_1_closed_2026_07_09": {
  "directive": "operator chose \"HIGH + my partials first\"",
  "closed": [
   "hostkit-rest HIGH-2",
   "orchestration HIGH-1",
   "runtimecore-a #1 (fix Mac-compiled, sleep-advance teeth device-gated)",
   "mlx-decode MED-1 complete (my partial)",
   "organ-eval MED-5 clause 2 (my partial)"
  ],
  "still_open": "the rest of the 113 gaps: ~1 CRITICAL residual (HF token history, operator-gated) + remaining FALSE_CLOSE/CLAIMED partials (memory-b F3, orchestration MED-2/MED-4, runtimecore-b MED-3) + ~28 MED + ~79 LOW silent-drops (99 Mac-fixable overall)."
 },
 "tranche_2_closed_2026_07_09": {
  "directive": "全面开发 — the 5 confirmed partials, full teeth + individual push",
  "closed": [
   {
    "id": "memory-b F3",
    "commit": "991750dd2",
    "note": "admit() confidence-tiebreak guards contentCache overwrite"
   },
   {
    "id": "orchestration MED-2",
    "commit": "64f2faf10",
    "note": "dominance-permutation validator, fail-safe to Swift sort (no process-abort)"
   },
   {
    "id": "organ-eval MED-2",
    "commit": "5f2f2dc10",
    "note": "AB fidelity anchor fail-closes when required-but-unverifiable"
   },
   {
    "id": "mlx-decode MED-1",
    "commit": "6e8e75655",
    "note": "mtpStreamHasRoom guards ALL 5 mtpForward sites (sampling/chain/greedy), not just fused chain"
   },
   {
    "id": "organ-eval MED-5 clause 2",
    "commit": "882fa3422",
    "note": "orderedFailureLabels — deterministic, not dict-order"
   },
   {
    "id": "runtimecore-b MED-3",
    "commit": "9ddad0695",
    "note": "KG reads surface busy/corrupt via onSilentFailure + busy_timeout=5000 + allNodesOrThrow/allEdgesOrThrow; read-time teeth device-gated (structural corruption caught at OPEN), sibling event-log test carries reversal-red"
   },
   {
    "id": "orchestration MED-4 (streaming facet)",
    "commit": "d22985751",
    "note": "sink-throw wrapped in SinkNotificationError carrying committed result → re-deliver, no double-write retry"
   }
  ],
  "still_open": "~1 CRITICAL (HF token history, operator-gated) + ~7 distinct Mac-fixable MED (hostkit-rest MED-2 count-inflation, TrainingDataExporter atomic-rename, hostkit-spine F3 projection-divergence, memory-b F5 warm-cache reentrancy, tests-arch ②b permatrue, tests-arch ④ iOS source-gate, x-test-integrity F5 saturated probes) + ~5 device/hardware-gated MED (MED-8/MED-9/memory-a F4 VACUUM/x-sov #5/x-concurrency) + tools-scripts R2/R3 (operator experiment domain, do not touch) + ~79 LOW."
 },
 "tranche_3_closed_2026_07_10": {
  "directive": "全面开发 — the 7 verified Mac-fixable MED (read-only verification workflow weprgk00n first re-derived each from HEAD)",
  "method": "7-agent read-only verification workflow confirmed each gap still-open at HEAD before any edit (re-derive from source, not accounting)",
  "closed": [
   {
    "id": "hostkit-rest MED-2 (count inflation)",
    "commit": "4af0069d5",
    "note": "submitWithForbiddenZoneGate returns TRUE persisted state via entry(ticketID:), not hardcoded .proposed → already-rejected duplicate no longer inflates accept count"
   },
   {
    "id": "TrainingDataExporter atomic-rename (§0 comment-lie)",
    "commit": "c6fd1ebee",
    "note": "commit extracted to injectable atomicCommitStrategy (replaceItemAt safe-save); old removeItem-then-moveItem had a destination-gone window despite the Atomic rename comment"
   },
   {
    "id": "memory-b F5 (warm-cache reentrancy)",
    "commit": "f4965c804",
    "note": "warmCacheIfNeeded computes into locals + flips hasWarmedCache last (re-check); append re-projects on seq gap. Was: flag-before-populate dropped a concurrent mutation. Opt-in .warmAtInit path"
   },
   {
    "id": "tests-arch ②b (XCTAssertTrue(true) tautologies)",
    "commit": "cf04f799d",
    "note": "BASTautologyBudgetLintTests ratchet freezes the debt as a monotonically-decreasing budget; converted 3 signature-freeze sites to M824 compile-time-proof, 50→47"
   },
   {
    "id": "x-test-integrity F5 (saturated reading probe)",
    "commit": "39e074027",
    "note": "CRITICAL gate #20 counterfactual_lift repointed off the saturated soft probe onto the discriminating hard counterfactual probe (follow/n, computed provenance). #21/#26 tracked residual. threshold >0.35 unchanged (calibration a documented follow-up)"
   },
   {
    "id": "tests-arch ④ (iOS source-gate)",
    "commit": "c401168b1",
    "note": "anti-silent-drift tripwire confirmed live+correct; generalized its self-exclusion to lint-infrastructure so the ②b lint keeps the behavioral count honest at 161. Un-gating the 161 = separate device/infra campaign (deferred)"
   }
  ],
  "not_open": [
   {
    "id": "hostkit-spine F3 (projection divergence)",
    "verdict": "NOT AN OPEN DEFECT — verified misdescribed; the permit.mode-escalation precondition cannot exist (red-line-8 tone is a pure fn of riskLevel/permitMode/candidateCount; anchorID/hostSummaryRef do not affect it)"
   }
  ],
  "still_open": "~1 CRITICAL (HF token history, operator-gated) + ~5 device/hardware-gated MED (MED-8/MED-9 decoder re-build, memory-a F4 VACUUM, x-sov #5 at-rest, x-concurrency semaphore) + tools-scripts R2/R3 (operator experiment domain, do not touch) + tests-arch ④ un-gating (device/infra campaign) + #21/#26 reading-probe discriminating repoint (eval-validity program) + ~79 LOW."
 }
}